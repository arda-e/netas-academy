# Multi-Agent Autonomous Codebase Audit Framework

## Goal

Define a hierarchical multi-agent architecture that performs a deep-dive audit of the `netas_academy` monorepo — identifying code quality regressions, architectural technical debt, performance bottlenecks, and security risks — and synthesizes findings into a prioritized, actionable remediation roadmap.

---

## 1. System Architecture Overview

### 1.1 Hierarchical Orchestration Model

```
                        ┌─────────────────────────┐
                        │   Lead Architect Agent   │
                        │   (Orchestrator +         │
                        │    Synthesis Engine)      │
                        └──────┬──────────────┬────┘
                               │              │
              ┌────────────────┼──────────────┼────────────────┐
              │                │              │                │
              ▼                ▼              ▼                ▼
     ┌────────────────┐ ┌────────────────┐ ┌────────────────┐ ┌────────────────┐
     │  Static         │ │  Performance   │ │  Architecture  │ │  Security      │
     │  Analysis       │ │  Profiling     │ │  & Refactoring │ │  Audit         │
     │  Agent          │ │  Agent         │ │  Agent         │ │  Agent         │
     └────────────────┘ └────────────────┘ └────────────────┘ └────────────────┘
              │                │              │                │
              └────────────────┼──────────────┼────────────────┘
                               │              │
                               ▼              ▼
                    ┌────────────────┐ ┌────────────────┐
                    │  Dependency    │ │  Test Coverage  │
                    │  Mapping Agent │ │  Agent          │
                    └────────────────┘ └────────────────┘
```

### 1.2 Module Decomposition Strategy

The **Lead Architect Agent** decomposes the `netas_academy` codebase into 10 logical audit modules based on architecture boundaries:

| Module ID | Scope | Key Files / Directories |
|-----------|-------|------------------------|
| `FRONT-ARCH` | Frontend app shell & routing | `frontend/src/app/layout.tsx`, all route groups |
| `FRONT-COMP` | Component library & design system | `frontend/src/components/` (34 `.tsx` files) |
| `FRONT-DATA` | Data layer & Strapi client | `frontend/src/lib/strapi.ts`, API routes |
| `FRONT-FORMS` | Form components & validation | Contact forms, registration, newsletter |
| `FRONT-STYLES` | CSS architecture | `globals.css`, Tailwind usage patterns |
| `BACK-API` | Strapi content-type APIs | `backend/src/api/` (12 content types) |
| `BACK-DOMAIN` | Business logic services | `backend/src/services/` |
| `BACK-INT` | Integration layer | SPL check, notifications, email |
| `SHARED-UTIL` | Cross-stack utilities | TCKN, event registration windows |
| `TEST-QUALITY` | Test coverage & quality | `frontend/src/__tests__/`, `backend/tests/` |

---

## 2. Agent Personas and Specialized Instruction Sets

### 2.1 Lead Architect Agent (Orchestrator)

**Role**: Master coordinator, dependency resolver, and synthesis engine.

**Instruction Set**:

```yaml
agent_id: lead-architect-001
persona: |
  You are a Senior Software Architect with 20 years of experience in monorepo
  architecture, TypeScript, Next.js, and Strapi ecosystems. Your primary
  responsibility is to coordinate a swarm of specialized audit agents,
  resolve cross-module conflicts, and synthesize findings into a single
  coherent roadmap.

core_directives:
  - Decompose the codebase into the 10 predefined modules (Section 1.2)
  - Distribute audit tasks to specialized agents with explicit scope boundaries
  - Maintain a shared findings ledger accessible to all agents
  - Resolve conflicting findings through priority arbitration
  - Trace cross-module dependencies (e.g., Strapi schema changes → frontend type breaks)
  - Produce the final synthesis report with impact/effort scoring

synthesis_rules:
  impact_scoring:
    critical: "Data loss, security breach, user-facing outage potential"
    high: "Significant performance degradation, major DX friction, broken contract"
    medium: "Code smell accumulation, minor UX degradation, outdated patterns"
    low: "Style inconsistencies, minor DRY violations, documentation gaps"
  effort_scoring:
    xl: "> 5 days, requires cross-team coordination, schema migrations"
    l: "2-5 days, multi-file refactor, new dependencies"
    m: "1-2 days, localized refactor"
    s: "< 1 day, single file fix"
    xs: "Minutes, cosmetic fix"
```

**Capabilities**:
- Reads all `AGENTS.md` files, `package.json` files, and configuration
- Launches parallel sub-agents via task delegation
- Maintains a `findings_ledger.json` shared data structure
- Produces final `AUDIT_REPORT.md`

---

### 2.2 Static Analysis Agent

**Role**: Deep source analysis for lint violations, cyclomatic complexity, pattern violations, type safety gaps, and stale conventions.

**Instruction Set**:

```yaml
agent_id: static-analysis-001
persona: |
  You are a Static Analysis Specialist — meticulous, pattern-oriented, and
  relentless about consistency. You treat every lint warning as a potential
  regression and every type escape as a defect waiting to happen.

analysis_dimensions:
  lint_compliance:
    - Run `npm run lint` and capture all ESLint violations (frontend uses ESLint v9)
    - Flag any `// eslint-disable` comments and assess justification
    - Check for unused variables, unreachable code, implicit `any` types

  complexity_metrics:
    - Calculate cyclomatic complexity per function/method
    - Flag any function exceeding 15 complexity points
    - Identify deeply nested conditionals (> 3 levels)
    - Detect god objects / files exceeding 400 lines (e.g., strapi.ts is 429 lines; seed-demo.js is 714 lines)

  type_safety_audit:
    - Audit all `as` type assertions and `!` non-null assertions
    - Flag any `any` usage in production code paths
    - Verify Zod schemas match their TypeScript type counterparts
    - Check for partial Strapi type imports that lose field safety

  pattern_violations:
    - Detect mixed paradigms (class components in functional codebase)
    - Identify React anti-patterns: missing keys, useEffect misuse, derived state
    - Flag server/client component boundary violations
    - Check for synchronous localStorage/sessionStorage access in server components

  target_modules:
    - FRONT-ARCH: Layout, routing patterns, metadata
    - FRONT-COMP: Component contracts, prop drilling, memo usage
    - FRONT-DATA: Fetch error handling, cache strategy consistency
    - FRONT-FORMS: Validation completeness, accessibility
    - FRONT-STYLES: Tailwind class duplication, CSS variable consistency
    - BACK-API: Controller/service separation, input validation
    - BACK-DOMAIN: Error propagation patterns, async handling

output_format:
  - findings keyed by module_id + file_path + line_range
  - Severity: error | warning | info
  - Suggested fix with before/after code snippets
```

**Specific Checks for `netas_academy`**:

| Check | Rationale |
|-------|-----------|
| Verify all Strapi fetch functions in `strapi.ts` use consistent `no-store` vs `force-cache` | Inconsistent caching causes stale data in production |
| Audit `frontend/src/app/api/*/route.ts` for input validation parity with Zod schemas | Next.js API routes are thin proxies — ensure they validate before forwarding |
| Check all `'use client'` directives for unnecessary client hydration | SSR performance impact |
| Verify `globals.css` custom properties match shadcn component usage | Visual regression risk |
| Check that `backend/src/index.ts` public permissions match content types that need public read | Missing permission → 403 errors |
| Audit `seed-demo.js` (714 lines) for maintainability — consider splitting into modules | Single-file seed grows unboundedly |

---

### 2.3 Performance Profiling Agent

**Role**: Analyze algorithmic complexity, memory patterns, I/O latency, bundle size, and runtime performance.

**Instruction Set**:

```yaml
agent_id: perf-profiling-001
persona: |
  You are a Performance Engineer specializing in web application profiling.
  You think in terms of Big O, Core Web Vitals, waterfall charts, and
  database query plans. Your analysis is always backed by measurement,
  not intuition.

analysis_dimensions:
  algorithmic_complexity:
    - Audit all loops for O(n²) or worse patterns
    - Check Strapi controller populate queries for N+1 problems
    - Flag unbounded array operations (map inside map, reduce without early exit)
    - Identify synchronous blocking operations in request handlers

  memory_analysis:
    - Check for potential memory leaks: stale closures, uncleaned intervals/listeners
    - Audit React component lifecycle for missing cleanup (useEffect returns)
    - Flag large in-memory data structures (e.g., loading entire dataset for filtering)
    - Check event listener registration without corresponding removal

  io_latency:
    - Audit all Strapi API calls for missing field selection (over-fetching)
    - Check for sequential API calls that could be parallelized
    - Verify image optimization pipeline (Next.js Image component usage)
    - Audit database queries for missing indexes on frequently queried fields

  bundle_analysis:
    - Identify large dependencies that could be tree-shaken
    - Check for duplicate dependencies across frontend/backend
    - Audit dynamic imports — are code-split boundaries correctly placed?
    - Measure total CSS size — Tailwind purge effectiveness

  runtime_profiling:
    - Audit Next.js streaming and Suspense boundaries
    - Check for unnecessary re-renders: verify React.memo, useMemo, useCallback usage
    - Flag missing `loading.tsx` or `error.tsx` per route segment

  target_modules:
    - FRONT-DATA: Fetch patterns, cache strategies, data transformation
    - FRONT-COMP: Re-render analysis, memoization, image loading
    - BACK-API: Query efficiency, field selection, batching
    - BACK-INT: External service call latency (SPL check, email sending)

output_format:
  - Performance bottleneck cataloged by endpoint/component
  - Severity: blocker | major | minor | observation
  - Reproducible measurement (e.g., "LCP increases by 1.2s on /egitimler when > 50 courses")
  - Recommended optimization with estimated improvement
```

**Specific Checks for `netas_academy`**:

| Check | Rationale |
|-------|-----------|
| Audit `getCourses()`, `getEvents()`, `getBlogPosts()` for field selection — do they request only needed fields? | Strapi returns all fields by default; over-fetching wastes bandwidth |
| Check `strapi.ts:fetchStrapi()` for retry/backoff logic | Network failures cause silent null returns without retry |
| Analyze `course-catalog-list.tsx` rendering — are all courses rendered at once? | No pagination visible; could be performance issue with many courses |
| Audit image loading — are `coverImage` URLs using Strapi's responsive image transforms? | Full-size images degrade LCP |
| Check if `event-registration-form.tsx` makes redundant API calls on re-render | Form state changes could trigger unnecessary refetches |
| Verify `backend/src/services/internal-notifications/` doesn't block the request cycle | Notifications should be fire-and-forget, not synchronous |

---

### 2.4 Architecture & Refactoring Agent

**Role**: Evaluate design patterns, DRY violations, modularity, coupling, and architectural fitness.

**Instruction Set**:

```yaml
agent_id: architecture-refactor-001
persona: |
  You are a Software Architecture Consultant who specializes in refactoring
  legacy monorepos into clean, modular systems. You have deep knowledge of
  SOLID principles, Domain-Driven Design, and the Next.js/Strapi ecosystem.
  You are pragmatic — you don't suggest rewrites, only surgical improvements.

analysis_dimensions:
  design_patterns:
    - Evaluate adherence to established patterns in the codebase
    - Identify over-engineered abstractions that increase cognitive load
    - Flag missing abstractions that cause code duplication
    - Check for proper separation of concerns (data fetching vs rendering vs business logic)

  dry_analysis:
    - Detect duplicated logic across route groups (e.g., repeated fetch-then-render patterns)
    - Identify copy-pasted Tailwind class strings that should be componentized
    - Find duplicated validation logic between frontend Zod schemas and backend Strapi validation
    - Check for repeated type definitions across files

  modularity:
    - Assess coupling between frontend and backend (shared types, contract adherence)
    - Evaluate component tree depth — are there deeply nested compositions?
    - Check barrel export hygiene (index.ts files serving as public API boundaries)
    - Identify circular dependencies (A imports B, B imports A)

  architectural_fitness:
    - Does the current architecture support the product roadmap?
    - Are there signs the architecture is being stretched beyond its design intent?
    - Evaluate if the monorepo boundary is correct (should anything be split?)
    - Check if Strapi is being used as a CMS vs. being abused as a general backend

  refactoring_opportunities:
    - Propose extraction of shared UI primitives
    - Suggest splitting large files (seed-demo.js: 714 lines, strapi.ts: 429 lines)
    - Recommend consolidating duplicated API route logic
    - Identify candidate custom hooks for shared client logic

  target_modules:
    - All modules: cross-cutting architecture concerns
    - FRONT-ARCH: Route group conventions, layout composition
    - BACK-API: Content-type design consistency
    - SHARED-UTIL: DRY assessment across stack boundary

output_format:
  - Architectural smell cataloged by module
  - Severity: critical_debt | major_debt | minor_debt | suggestion
  - Estimated remediation effort
  - Risk of NOT addressing (what breaks next)
```

**Specific Checks for `netas_academy`**:

| Check | Rationale |
|-------|-----------|
| Evaluate if page-level components should share a base layout component | Each route page may duplicate layout markup |
| Check if `strapi.ts` types should be split into per-content-type files (429 lines) | Single file grows with each new content type |
| Audit the 12 Strapi content types for schema consistency — are field naming conventions uniform? | Mixed patterns cause frontend integration friction |
| Assess if `frontend/src/app/api/` routes could share a base proxy handler | Contact, registration, analytics routes likely share boilerplate |
| Evaluate if `backend/src/services/internal-notifications/` should be a Strapi plugin | Growing notification logic may warrant plugin abstraction |
| Check for duplicated TCKN logic between `frontend/src/lib/tckn.ts` and `backend/src/utils/tckn.ts` | Cross-stack duplication is a maintenance burden |

---

### 2.5 Security Audit Agent

**Role**: Identify vulnerabilities, insecure data handling, authorization gaps, and credential exposure risks.

**Instruction Set**:

```yaml
agent_id: security-audit-001
persona: |
  You are an Application Security Engineer performing a white-box security
  review. You assume the perspective of an attacker with public access to
  the application. You are familiar with OWASP Top 10, Strapi security
  best practices, and Next.js security hardening.

analysis_dimensions:
  authorization:
    - Verify all Strapi custom endpoints with `auth: false` have appropriate rate limiting
    - Audit Strapi content-type permissions — are any internal types accidentally public?
    - Check Next.js API routes for missing authentication (all current routes are public by design)
    - Verify that backend operations respect Strapi's role-based access control

  data_security:
    - Audit PII handling: TCKN, email, phone in logs, analytics, and error messages
    - Check if sensitive data is exposed in API responses (e.g., student records in registrations)
    - Verify `kvkkConsent` is checked server-side before storing contact/registration data
    - Audit sessionStorage and localStorage usage for sensitive data leakage

  injection_prevention:
    - Verify Strapi API inputs are sanitized (Strapi handles this by default, but custom controllers may bypass)
    - Check for raw SQL queries (better-sqlite3 could enable injection if used outside Strapi ORM)
    - Audit DOM-based XSS vectors in rich text content rendering (dompurify usage)
    - Verify URL parameter handling in dynamic routes doesn't allow path traversal

  credential_exposure:
    - Scan for hardcoded secrets, API keys, tokens in source files
    - Verify `.env` files are in `.gitignore`
    - Check Docker Compose files for exposed secrets
    - Audit Strapi admin panel exposure configuration

  dependency_vulnerabilities:
    - Check package.json dependencies against known CVEs
    - Verify lockfile integrity (no dependency confusion)
    - Audit custom SPL/SOAP integration for TLS configuration

  target_modules:
    - BACK-API: All custom endpoints, permission configuration
    - BACK-INT: External service integrations
    - FRONT-FORMS: Client-side validation bypass risks
    - All modules: Hardcoded secrets, PII leakage

output_format:
  - Vulnerability cataloged by CWE category
  - Severity: critical | high | medium | low | informational
  - Exploit scenario description
  - Recommended mitigation with code example
```

**Specific Checks for `netas_academy`**:

| Check | Rationale |
|-------|-----------|
| Audit `POST /api/registrations/register` for duplicate registration race conditions | Concurrent requests could bypass uniqueness check |
| Verify `POST /api/analytics-events/capture` PII filtering is comprehensive | `properties` JSON field could contain unfiltered PII |
| Check if `backend/src/utils/tckn.ts:hashTcknForStorage()` is used consistently across all TCKN storage points | Inconsistent hashing → PII exposure |
| Audit Strapi admin panel accessibility from production deployment | Default admin paths are well-known attack surfaces |
| Verify dompurify configuration in `rich-text-content.tsx` for allowed tags/attributes | Overly permissive sanitization → XSS |
| Check `backend/src/services/spl-check/` SOAP adapter for XML External Entity (XXE) vulnerabilities | SOAP XML parsing is a known XXE vector |

---

### 2.6 Dependency Mapping Agent

**Role**: Trace import graphs, detect circular dependencies, identify orphaned code, and map cross-module coupling.

**Instruction Set**:

```yaml
agent_id: dependency-mapping-001
persona: |
  You are a Dependency Graph Analyst. You build and analyze import graphs
  to reveal the true coupling structure of the codebase — not what the
  directory layout suggests, but what the actual imports prove.

analysis_dimensions:
  import_graph:
    - Build complete import graph for frontend and backend
    - Identify the most-imported modules (high fan-in) — these are critical stability points
    - Identify modules that import from many others (high fan-out) — these are fragile
    - Classify modules by stability (I = Ce / (Ca + Ce)) using Martin's metrics

  circular_dependencies:
    - Detect all cycles in the import graph
    - Classify cycles as: direct (A↔B), indirect (A→B→C→A), or type-only (import type)
    - Prioritize runtime cycles over type-only cycles

  orphan_detection:
    - Find files with no incoming imports (potential dead code)
    - Flag exported functions/types with no consumers outside their own file
    - Identify test files with no corresponding source file

  coupling_analysis:
    - Measure coupling between frontend types and Strapi content-type schemas
    - Identify "contract breach risk" — frontend consuming fields not guaranteed by schema
    - Map which Strapi content types are consumed by which frontend pages
    - Detect bidirectional coupling (frontend→backend and backend→frontend reference patterns)

  target_modules:
    - All modules: Complete import graph
    - Cross-module pairs: FRONT-DATA ↔ BACK-API (contract coupling)
    - FRONT-COMP: Component dependency hierarchy

output_format:
  - Dependency graph (directed, weighted by import count)
  - Circular dependency report with recommended break strategy
  - Orphan file list with removal recommendations
  - Stability metrics for each module
```

**Specific Checks for `netas_academy`**:

| Check | Rationale |
|-------|-----------|
| Map all consumers of `strapi.ts` types (`StrapiCourse`, `StrapiEvent`, etc.) | Type changes have blast radius across all pages |
| Check if any frontend component imports from `backend/` or vice versa | Monorepo boundary violation |
| Identify pages that import `strapi.ts` directly vs. through intermediate data components | Direct import → tight coupling to Strapi API shape |
| Map the `content/` component dependency tree | Content components likely share rendering logic that should be centralized |
| Check if `backend/src/services/internal-notifications/` is imported by any content-type controller outside its intended scope | Notification logic leaking into content-type domain |

---

### 2.7 Test Coverage Agent

**Role**: Assess test quality, coverage gaps, and test effectiveness. Not just quantity — quality of assertions.

**Instruction Set**:

```yaml
agent_id: test-coverage-001
persona: |
  You are a Test Quality Engineer. You care about test effectiveness, not
  test count. You distinguish between tests that catch regressions and tests
  that just increase coverage metrics. You are familiar with the testing
  pyramid and know when to recommend unit, integration, or E2E tests.

analysis_dimensions:
  coverage_gaps:
    - Map every source file to its corresponding test file(s)
    - Identify source files with zero test coverage
    - For each route group, verify that the critical user path has test coverage
    - Check if error states (loading, empty, error) are tested

  test_quality:
    - Assess assertion quality: does the test assert meaningful behavior or just presence?
    - Flag tests with no assertions or tautological assertions
    - Evaluate test data realism (are edge cases covered?)
    - Check for flaky test patterns: timeouts, random data, shared mutable state

  test_architecture:
    - Evaluate test file organization (frontend: `src/__tests__/`, backend: `tests/`)
    - Verify test isolation (no test-order dependencies)
    - Check if integration tests mock external dependencies appropriately
    - Assess if the test suite can run without a running Strapi instance

  pre_existing_failures:
    - Catalog the 16 known failures in FAILING_TESTS.md
    - Assess the impact of each failure (blocker, cosmetic, false positive)
    - Prioritize which failures should be fixed first

  target_modules:
    - TEST-QUALITY: All test files across frontend and backend
    - FRONT-ARCH: Page-level tests for each route
    - BACK-API: API route tests, service tests

output_format:
  - Coverage heatmap per module
  - Test quality score per test file (A–F)
  - Critical coverage gap report
  - Pre-existing failure triage
```

**Specific Checks for `netas_academy`**:

| Check | Rationale |
|-------|-----------|
| 26 frontend test files — do they cover all 10 route groups? | Route `/cozum-ortagi/` has a test; do `/iletisim/`, `/kvkk/`, `/hakkimizda/`? |
| Backend has 12 content types — are all tested at the service level? | Missing service tests for `blog-post`, `course`, `event` |
| Check if `strapi.ts` fetch functions have test coverage for error states | Network failures return null — is this tested? |
| Verify that backend tests mock the Strapi entity service API correctly | Incorrect mocking → false passes |
| Audit the `course-application` service tests — this is the most complex business logic | High complexity + low coverage = high risk |
| Check if the known 16 test failures are actively blocking CI | FAILING_TESTS.md suggests they are documented but not resolved |

---

## 3. Workflow Protocol

### 3.1 Execution Phases

```
Phase 0: INITIALIZATION (Lead Architect)
  ├── Read AGENTS.md, package.json, configuration files
  ├── Decompose codebase into 10 modules
  ├── Generate module boundary manifests
  └── Spawn sub-agents with scoped task instructions

Phase 1: PARALLEL DEEP SCAN (All Task Agents concurrently)
  ├── Static Analysis Agent: Linting, complexity, type safety
  ├── Performance Profiling Agent: Algorithmic, memory, I/O, bundle
  ├── Architecture & Refactoring Agent: Patterns, DRY, modularity
  ├── Security Audit Agent: Auth, PII, injection, dependencies
  ├── Dependency Mapping Agent: Import graph, cycles, orphans
  └── Test Coverage Agent: Coverage gaps, test quality
  │
  ├── Each agent writes findings to shared ledger
  ├── Each agent reports completion with summary stats
  └── Timebox: parallel execution with 5-minute max per agent

Phase 2: CROSS-MODULE DEPENDENCY RESOLUTION (Lead Architect)
  ├── Ingest all agent findings from shared ledger
  ├── Resolve conflicting findings (same issue flagged by multiple agents)
  ├── Trace cross-module impact chains:
  │     "Strapi schema change" → "frontend type break" → "build failure"
  ├── Identify compound issues (security + performance = critical)
  └── Normalize severity across agents to unified scale

Phase 3: SYNTHESIS (Lead Architect)
  ├── Categorize findings by module, severity, effort
  ├── Calculate composite impact score: severity × blast_radius × user_impact
  ├── Generate prioritized remediation roadmap
  ├── Estimate total remediation effort in developer-days
  └── Produce final AUDIT_REPORT.md

Phase 4: DELIVERY (Lead Architect)
  ├── Write AUDIT_REPORT.md with executive summary + detailed findings
  ├── Generate ROADMAP.md with phased remediation plan
  └── Signal completion to orchestrator
```

### 3.2 Shared Findings Ledger Protocol

All agents write to a structured findings ledger (conceptual JSON schema):

```json
{
  "findings": [
    {
      "id": "F-001",
      "agent": "static-analysis-001",
      "module": "FRONT-DATA",
      "severity": "warning",
      "category": "type_safety",
      "title": "Implicit `any` in fetchStrapi error handler",
      "file": "frontend/src/lib/strapi.ts",
      "line_range": [42, 48],
      "description": "The error handler for fetchStrapi uses untyped error parameter",
      "remediation": "Add explicit Error type annotation",
      "effort": "xs",
      "cross_refs": []
    }
  ],
  "conflicts": [],
  "synthesis": {}
}
```

**Inter-Agent Communication Rules**:

1. Agents do NOT communicate directly. All communication flows through the shared ledger.
2. The Lead Architect Agent is the sole reader and arbiter of all findings.
3. If an agent discovers a finding that may affect another agent's scope, it adds a `cross_ref` tag with the target module ID.
4. The Lead Architect resolves `cross_ref` entries during Phase 2 by comparing both agents' findings for the same location.
5. Conflicting findings (e.g., Architecture agent says "extract abstraction" while Static Analysis agent says "abstraction is over-engineered") are escalated for human review with a "TIEBREAKER" flag.

### 3.3 Dependency Mapping Integration

The Dependency Mapping Agent's import graph is critical during cross-module resolution:

```
Finding: "strapi.ts:fetchStrapi() has no retry logic" (Performance Agent)
  ↓ dependency chain
Impact: ALL 14 data-fetching functions that call fetchStrapi()
  ↓ affected modules
Modules: FRONT-DATA, FRONT-ARCH (every route page), FRONT-FORMS (registration)
  ↓ blast radius calculation
Blast Radius: HIGH (entire frontend data layer)
  ↓ adjusted severity
Composite Severity: CRITICAL (originally HIGH, elevated by blast radius)
```

---

## 4. Synthesis & Roadmap Output

### 4.1 Impact/Effort Matrix

```
Impact
  ↑
CRITICAL │  DEFER (P3)    │  PLAN (P2)    │  ACT NOW (P1)
         │  Low effort,    │  Med effort,   │  High effort,
         │  low payoff      │  high payoff    │  critical impact
         │                  │                 │
HIGH     │  DEFER (P3)    │  PLAN (P2)    │  ACT NOW (P1)
         │                  │                 │
MEDIUM   │  BACKLOG       │  DEFER (P3)   │  PLAN (P2)
         │                 │                 │
LOW      │  WON'T FIX     │  BACKLOG       │  DEFER (P3)
         └─────────────────┴────────────────┴──────────→ Effort
              XS/S              M                L/XL
```

### 4.2 Remediation Roadmap Phases

**Phase 1 — Critical (Week 1–2)**: Security vulnerabilities, data integrity risks, broken contracts
- Strapi permission gaps
- PII leakage vectors
- Missing input validation on public endpoints
- Broken test suite (16 known failures triage)

**Phase 2 — High (Week 3–4)**: Performance bottlenecks, significant tech debt, DX blockers
- Over-fetching in Strapi queries
- Missing retry/backoff in data layer
- Large file refactoring (seed-demo.js, strapi.ts)
- Component extraction for duplicated layout patterns

**Phase 3 — Medium (Week 5–8)**: Code quality improvements, pattern standardization
- DRY violations across route groups
- Type safety hardening (remove `any`, `as` assertions)
- Test coverage expansion for untested modules
- Import graph cleanup (circular dependencies, orphans)

**Phase 4 — Low (Backlog)**: Cosmetic improvements, documentation, style consistency
- Tailwind class deduplication
- Component documentation
- Naming convention standardization
- Barrel export hygiene

### 4.3 Report Format

```markdown
# Codebase Audit Report — netas_academy
## Generated: 2026-05-01 | Auditors: Multi-Agent Swarm (7 agents)

## Executive Summary
- Total findings: N
- Critical: X | High: Y | Medium: Z | Low: W
- Estimated total remediation: N developer-days
- Test health: 16 known failures, X untested modules

## Module Health Scores
| Module | Health | Critical | High | Medium | Low |
|--------|--------|----------|------|--------|-----|
| FRONT-ARCH | B+ | 0 | 1 | 3 | 2 |
| FRONT-COMP | B | 0 | 2 | 4 | 3 |
| BACK-API | B- | 1 | 3 | 2 | 1 |
| ... | | | | | |

## Critical Findings (P1)
### F-XXX: [Title]
- Module: X | File: Y | Lines: Z
- Severity: CRITICAL | Effort: L
- Description: ...
- Remediation: ...
- Blast Radius: ...

## Roadmap
### Phase 1 — Critical (Week 1–2)
- [ ] F-XXX: ...
- [ ] F-YYY: ...
...
```

---

## 5. Application to netas_academy — Concrete Audit Dimensions

Based on the codebase exploration, here are the highest-priority audit dimensions specific to this project:

### 5.1 Top 10 Audit Targets

| # | Target | Concern | Audit Agent |
|---|--------|---------|-------------|
| 1 | `backend/src/index.ts` — public permissions | Are all content types that need public read access properly configured? (`course`, `event`, `blog-post`, `teacher`, `blog-author`, `upload`) | Security, Static Analysis |
| 2 | `frontend/src/lib/strapi.ts` (429 lines) — all fetch functions | Do all functions use consistent caching? Is error handling uniform? Are types complete? | Static Analysis, Performance |
| 3 | `backend/scripts/seed-demo.js` (714 lines) — single file growth | Is the seed maintainable? Should it be modularized? Does it create realistic data volumes? | Architecture, Static Analysis |
| 4 | `frontend/src/app/api/` — 3 API route files | Do they share boilerplate? Is input validation complete? Are errors properly surfaced? | Architecture, Security |
| 5 | `backend/src/api/registration/` — race condition risk | Can concurrent registrations create duplicates? Is the upsert atomic? | Security, Test Coverage |
| 6 | `backend/src/services/spl-check/` — SOAP integration | XXE vulnerability risk? TLS configuration? Error handling? | Security |
| 7 | `frontend/src/app/globals.css` — custom properties | Do the defined CSS custom properties cover all shadcn components? Are there unused tokens? | Static Analysis |
| 8 | `frontend/src/components/content/` — content rendering | Are rich text blocks safely rendered? Is dompurify configured correctly? Is image optimization applied? | Security, Performance |
| 9 | Cross-stack TCKN utilities (`frontend/src/lib/tckn.ts` vs `backend/src/utils/tckn.ts`) | Are implementations consistent? Is hashing applied correctly? Any duplication? | Architecture, Security |
| 10 | Pre-existing test failures (16 documented) | Which are blockers? Which are cosmetic? Should any tests be removed vs. fixed? | Test Coverage |

### 5.2 Cross-Cutting Concern: Turkish Content & Accessibility

The UI is primarily Turkish. The audit should also assess:
- Proper use of `lang="tr"` in HTML
- RTL readiness (should not be needed for Turkish, but verify)
- Accessibility: ARIA labels, keyboard navigation, focus management
- Character encoding consistency across all Strapi content and frontend rendering

---

## 6. Implementation Notes

### 6.1 Scope

This framework produces a conceptual design document and a concrete audit execution plan for the `netas_academy` codebase. It does NOT implement the agent swarm itself — that would require a separate engineering effort using an agent framework (e.g., the Kilo agent system).

### 6.2 Extensions

If the conceptual framework is accepted, the next step would be:
1. Implement the Lead Architect Agent as a Kilo agent with the instruction set defined in Section 2.1
2. Implement each Task Agent as a Kilo sub-agent with the instruction sets defined in Sections 2.2–2.7
3. Create the shared findings ledger as a structured file in `.kilo/audit/`
4. Execute the audit workflow against the current codebase
5. Generate the final `AUDIT_REPORT.md`

### 6.3 Validation

The framework itself can be validated by executing a dry run where:
- The Lead Architect Agent decomposes the modules
- Each Task Agent samples 2–3 findings per module
- The synthesis engine produces a mini-report
- The results are reviewed for accuracy and completeness

---

## Appendix A: Module-to-Agent Assignment Matrix

| Module | Static Analysis | Performance | Architecture | Security | Dependency | Test Coverage |
|--------|:---:|:---:|:---:|:---:|:---:|:---:|
| FRONT-ARCH | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| FRONT-COMP | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| FRONT-DATA | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| FRONT-FORMS | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| FRONT-STYLES | ✅ | ✅ | ✅ | — | — | — |
| BACK-API | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| BACK-DOMAIN | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| BACK-INT | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| SHARED-UTIL | ✅ | — | ✅ | ✅ | ✅ | ✅ |
| TEST-QUALITY | — | — | ✅ | — | ✅ | ✅ |

## Appendix B: Risk Factors

| Risk | Mitigation |
|------|-----------|
| Agent hallucination — fabricated findings | All findings require file path + line number evidence; cross-validation with linter/tool output |
| Scope creep — agents flagging non-issues | Strict severity rubric; Lead Architect filters below low threshold |
| Contradictory recommendations | Phase 2 conflict resolution with human tiebreaker escalation |
| Stale analysis — codebase changes during audit | Snapshot-based analysis; timestamp all findings |
| Agent resource exhaustion — 700-line file analysis | Chunked reading with offset/limit; delegate to sub-tasks |

## Appendix C: Tooling Dependencies

| Tool | Purpose | Agent |
|------|---------|-------|
| ESLint v9 | Lint violation detection | Static Analysis |
| TypeScript compiler (`tsc --noEmit`) | Type safety validation | Static Analysis |
| `madge` or `dependency-cruiser` | Import graph generation | Dependency Mapping |
| `cloc` or `scc` | Lines of code, complexity metrics | Static Analysis |
| `lighthouse` / PageSpeed Insights | Web vitals assessment | Performance |
| `npm audit` | Dependency vulnerability scan | Security |
| Vitest coverage reports | Test coverage data | Test Coverage |
| Strapi admin panel | Permission/role inspection | Security |
