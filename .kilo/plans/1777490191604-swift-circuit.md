# Explorer Inventory Pass — Unit 0 Complete

**Source plan:** `docs/plans/2026-04-29-015-testid-selector-instrumentation-plan.md`
**Status:** Explorer pass complete. All 5 sub-agents have returned inventories.
**Next step:** Implementer reviews these findings before proceeding to Unit 1.

---

## Agent 1: App Shell (`layout.tsx`, `site-header.tsx`, `site-footer.tsx`, `breadcrumbs.tsx`)

**Key findings (40+ items):**

| Surface | Key Items | High-confidence count |
|---|---|---|
| `layout.tsx` | `<main>` content slot (`root-layout.content`) | 1 useful |
| `site-header.tsx` | Logo link, mobile menu toggle, 8 desktop nav items, 8 mobile nav items | 18 high |
| `site-footer.tsx` | 8 site-plan links, 2 legal links | 10 high |
| `breadcrumbs.tsx` | `nav` landmark, dynamic crumb links, current-page span | 3 high |

**Notable:** All nav items derive route keys from `href` (e.g., `hakkimizda`, `egitimler`, `etkinlikler`, etc.). Mobile menu toggle has `aria-controls="site-mobile-navigation"` — must preserve. `aria-current` is already used for active nav state.

**Dynamic pattern:** `site-header.desktop-nav.{route-key}` and `site-header.mobile-nav.{route-key}`.

---

## Agent 2: Shared Content Components (`content/*`, `teacher-card.tsx`)

**Key finding: Shared components need an optional `testId` prop** that callers pass down for per-surface uniqueness. All inventoried:

| Component | Pattern | Dynamic key |
|---|---|---|
| `SearchField` | `search-field.toggle`, `search-field.input` | — |
| `ContentCardShell` | `{testId}` root, then `.{testId}.title`, `.{testId}.meta`, etc. | Slug from caller |
| `ContentGrid` | `content-grid.empty` (when `itemsCount === 0`) | — |
| `ContentPageShell` | `content-page-shell.breadcrumbs`, `.title`, `.description`, `.description-trailing` | — |
| `ContentDetailShell` | `content-detail-shell.breadcrumbs`, `.title`, `.summary`, `.meta`, `.after-content` | — |
| `CourseList` | `egitimler.card.{course.slug}` | course.slug |
| `EventList` | `etkinlikler.card.{event.slug}` | event.slug |
| `BlogList` | `blog-yazilari.card.{post.slug}` | post.slug |
| `NewsList` | `haberler.card.{item.id}` | item.id (no slug in NewsListItem) |
| `BlogDetail` | `blog-yazilari.detail.*` (custom page, not using ContentDetailShell) | — |
| `TeacherCard` | `egitmenler.card.{slug}` | teacher.slug |

**Recommended prop approach for shared components:**
```tsx
<ContentCardShell testId={`egitimler.card.${course.slug}`} ... />
```
Component applies `${testId}` to root, `${testId}.title` to title, etc.

---

## Agent 3: Route-Level Pages (15 pages scanned)

**~150 items identified across all pages.** Consistent `page.{route-key}.*` naming:

| Route | Root selector | Key dynamic selectors |
|---|---|---|
| `/` (home) | `page.home` | `page.home.hero.cta.corporate-training`, `.catalog` (has existing `data-measurement-id`) |
| `/hakkimizda` | `page.hakkimizda` | `teacher-carousel.card.{slug}`, `course-carousel.card.{slug}`, CTAs |
| `/egitimler` | `page.egitimler` | `search.*`, `filter.topic.{area}`, `catalog.card.{slug}`, `catalog.empty` |
| `/egitimler/[slug]` | `page.course-detail` | `teacher-link.{slug}`, `cta.corporate-request`, `related-event.{slug}`, sections |
| `/etkinlikler` | `page.etkinlikler` | `filter.type.{etkinlik/egitim/kurs}`, `sort.toggle`, `list.card.{slug}` |
| `/etkinlikler/[slug]` | `page.event-detail` | `register-cta`, `contact-cta`, `info-panel`, `registration-closed.*` |
| `/etkinlikler/[slug]/kayit` | `page.event-registration` | `form`, `form.field.*` (matches form inventory), `closed-state` |
| `/egitmenler` | `page.egitmenler` | `list.card.{slug}` |
| `/egitmenler/[slug]` | `page.teacher-detail` | `email`, `course.{slug}`, sections |
| `/blog-yazilari` | `page.blog` | `search.*`, `list.card.{slug}` |
| `/blog-yazilari/[slug]` | `page.blog-detail` | `related-posts.card.{slug}` |
| `/haberler` | `page.haberler` | `list.empty` (currently always empty) |
| `/iletisim` | `page.iletisim` | `form`, `form.tab.{leadType}`, `form.field.*`, `form.success.*` |
| `/cozum-ortagi` | `page.cozum-ortagi` | `area-card.{title-slug}`, `cta.apply` |
| `/kvkk` | `page.kvkk` | `external-link.netas`, `commit-id` (debug) |

**Critical existing hooks to preserve:**
- `data-measurement-id` on home hero CTAs (`home-hero-primary`, `home-hero-secondary`, `about_corporate_cta`, `about_education_cta`)
- `aria-current="page"` on active nav items and filter pills
- `aria-label` on search toggle, carousel buttons, breadcrumbs

---

## Agent 4: UI Primitives (`components/ui/*`)

**CRITICAL FINDING: All 18 exported UI primitive components already forward `data-testid` via `{...props}` spreading. NO modifications needed to any UI primitive file.**

| File | Components | Spreads props? | Needs change? |
|---|---|---|---|
| `avatar.tsx` | Avatar, AvatarImage, AvatarFallback, AvatarBadge, AvatarGroup, AvatarGroupCount | Yes | ❌ None |
| `badge.tsx` | Badge | Yes (+ Radix `Slot.Root`) | ❌ None |
| `button.tsx` | Button | Yes (+ Radix `Slot.Root`) | ❌ None |
| `card.tsx` | Card, CardHeader, CardTitle, CardDescription, CardAction, CardContent, CardFooter | Yes | ❌ None |
| `input.tsx` | Input (with `forwardRef`) | Yes | ❌ None |
| `separator.tsx` | Separator | Yes | ❌ None |
| `textarea.tsx` | Textarea (with `forwardRef`) | Yes | ❌ None |

**Callers can already do:** `<Button data-testid="submit-btn" />`, `<Input data-testid="email" />`, etc. and the attribute appears on the DOM element.

---

## Agent 5: Forms (`event-registration-form`, `contact/*`, `newsletter`)

**47 items across 4 files. 17 existing `id`/`htmlFor` pairs documented — ALL must be preserved.**

### Existing id/htmlFor pairs (preserved):

| File | id | Notes |
|---|---|---|
| `event-registration-form.tsx` | `firstName`, `lastName`, `email`, `phone`, `tckn`, `notes` | All have matching `htmlFor` |
| `event-registration-form.tsx` | `kvkkConsent` | Label wraps input (no explicit htmlFor) |
| `intent-lead-form.tsx` | `fullName`, `email`, `phone`, `company`, `message`, `kvkkConsent` | All have matching `htmlFor` |
| `intent-field-sections.tsx` | `interestTopic`, `expertiseAreas`, `companySize`, `partnershipDetails` | All have matching `htmlFor` |
| `newsletter-subscription-form.tsx` | **None** | Email input has no id/label — add both |

### Suggested selector patterns:

- `event-registration.form`, `.field.*`, `.submit`, `.success`, `.error`
- `contact-lead.form`, `.tab.{leadType}`, `.field.*`, `.error.*`, `.submit`, `.success*`, `.new-submission`
- `newsletter.form`, `.field.email`, `.submit`, `.success`, `.error`

**Contact tabs use stable LeadType keys:** `corporate_training_request`, `instructor_application`, `solution_partner_application`, `general_contact`.

---

## Summary for Implementer

1. **UI primitives need NO changes** — all forward `data-testid` already.
2. **~250 high-confidence candidate selectors** identified across all surfaces.
3. **Dynamic keys are slug/documentId/route-key/intent-key based** — no array indexes anywhere.
4. **Shared components need an optional `testId` prop** approach:
   - `ContentCardShell`, `ContentGrid`, `ContentPageShell`, `ContentDetailShell`
   - Callers provide context-specific testId; component derives child selectors from root.
5. **All existing accessibility hooks preserved:** `id`, `htmlFor`, `aria-*`, `data-measurement-id`.
6. **Newsletter form has no id/label on email input** — this should be added in Unit 5.
7. **`/haberler` page is currently always empty** — only `list.empty` selector needed.
8. **Course PDF download is `aria-disabled` placeholder** — selector for disabled state.

### Recommended Unit 1 adjustments based on findings:

- `testids.ts` helper should focus on: joining segments, normalizing Turkish characters to ASCII, omitting empty segments.
- UI primitive passthrough is already confirmed working — Unit 1's primitive work can be skipped or reduced to verification only.
- Focus Unit 1 effort on the helper + source tests.


---

# Sub-Agent Task Breakdown — Units 1–6

Each sub-agent works on **non-overlapping files** within its unit. Dependencies between sub-agents are explicit.

---

## Unit 1: Selector Helper (1 sub-agent, sequential after Unit 0 review)

**Depends on:** Unit 0 inventory (reviewed)
**Parallelizable within unit:** No (single task)

### Sub-agent 1.1 — `testids.ts` helper + source test

| Property | Value |
|---|---|
| **Type** | `general` (implements + writes test) |
| **Creates** | `frontend/src/lib/testids.ts`, `frontend/src/__tests__/testids-source.test.mjs` |
| **Does NOT touch** | Any UI primitive files (passthrough already confirmed by Unit 0) |

**Scope:**
1. Create `testids.ts` with functions:
   - `join(...segments: (string | undefined | null)[]): string` — joins segments with `.`, omits empty/falsy
   - `normalizeKey(raw: string): string` — lowercases, replaces spaces/hyphens with `-`, strips non-ASCII-safe chars, collapses consecutive separators
   - Exported as named exports, no default export, no React dependency
2. Create `testids-source.test.mjs` with assertions:
   - `join('a', 'b', 'c')` → `"a.b.c"`
   - `join('a', undefined, 'c')` → `"a.c"`
   - `join('a', '', 'b')` → `"a.b"`
   - `normalizeKey('Veri Analizi')` → `"veri-analizi"`
   - `normalizeKey('a--b__c')` → `"a-b-c"`
   - `normalizeKey('')` → `""`

**Validation:** `node frontend/src/__tests__/testids-source.test.mjs` passes.

---

## Unit 2: App Shell Selectors (2 parallel sub-agents, after Unit 1)

**Depends on:** Unit 1 (`testids.ts` helper available)
**Files touched:** `layout.tsx`, `site-header.tsx`, `site-footer.tsx`, `breadcrumbs.tsx`
**Zero file overlap** between sub-agents.

### Sub-agent 2A — Header + layout selectors

| Property | Value |
|---|---|
| **Type** | `general` (implements) |
| **Modifies** | `frontend/src/app/layout.tsx`, `frontend/src/components/site-header.tsx` |

**Scope:**
1. `layout.tsx`: Add `data-testid="root-layout.content"` to the `<main>`/`<div className="flex-1">` wrapper (the content slot between header and footer). Low-confidence items from inventory can be skipped.
2. `site-header.tsx`:
   - Logo link: `data-testid="site-header.logo-link"` on the `<Link href="/">`
   - Mobile menu toggle: `data-testid="site-header.mobile-menu.toggle"` — **preserve** `aria-controls="site-mobile-navigation"` and `aria-expanded`
   - Mobile nav container: `data-testid="site-header.mobile-nav"` on the `<nav id="site-mobile-navigation">`
   - Desktop nav `<nav>`: `data-testid="site-header.desktop-nav"`
   - Each desktop nav link: `data-testid={join('site-header', 'desktop-nav', routeKey)}` where `routeKey` is extracted from `href` (e.g., `item.href.replace('/', '').replace('/', '-')`)
   - Each mobile nav link: `data-testid={join('site-header', 'mobile-nav', routeKey)}`
   - **Must preserve:** `aria-current` logic on active links, `aria-label` on logo link

### Sub-agent 2B — Footer + breadcrumbs selectors

| Property | Value |
|---|---|
| **Type** | `general` (implements) |
| **Modifies** | `frontend/src/components/site-footer.tsx`, `frontend/src/components/breadcrumbs.tsx` |

**Scope:**
1. `site-footer.tsx`:
   - Site-plan nav: `data-testid="site-footer.site-plan"` on `<nav aria-label="Site planı">`
   - Each site-plan link: `data-testid={join('site-footer', 'site-plan', linkKey)}` where `linkKey` derived from href
   - Legal nav: `data-testid="site-footer.legal"` on `<nav aria-label="Yasal ve kurumsal bağlantılar">`
   - Each legal link: `data-testid={join('site-footer', 'legal', linkKey)}`
   - **Must preserve:** `aria-label`s on nav elements
2. `breadcrumbs.tsx`:
   - `<nav>`: `data-testid="breadcrumbs.nav"` — **preserve** `aria-label="Sayfa yolu"`
   - Each non-current crumb link: `data-testid={join('breadcrumbs', 'link', getRouteKey(item.href))}`
   - Current/last crumb `<span>`: `data-testid="breadcrumbs.current"` — **preserve** `aria-current="page"`
   - Need a helper to extract route key from href (e.g., `"/egitimler"` → `"egitimler"`, `"/"` → `"ana-sayfa"`)

**Validation for Unit 2:** After both sub-agents complete, verify with source test.

---

## Unit 3: Shared Content Selectors (2 phases, sequential phases, sub-agents parallel within phase)

**Depends on:** Unit 0 inventory + Unit 1 (`testids.ts` helper)

### Phase 3.1 — Add `testId` prop to shared shell/grid/search components (1 sub-agent)

**Agent 3.1**

| Property | Value |
|---|---|
| **Type** | `general` |
| **Modifies** | `ContentCardShell.tsx`, `ContentGrid.tsx`, `ContentPageShell.tsx`, `ContentDetailShell.tsx`, `SearchField.tsx` |

**Scope:**
1. `SearchField.tsx`:
   - Toggle button: `data-testid="search-field.toggle"` — **preserve** `aria-label="Ara"`
   - Input: `data-testid="search-field.input"` — **preserve** `placeholder="Ara..."`
2. `ContentCardShell.tsx`: Add optional `testId?: string` prop
   - Root `<Link>` or `<Card>`: `data-testid={testId}` (only when `testId` provided)
   - Title `<CardTitle>`: `data-testid={testId && `${testId}.title`}`
   - Image `<Image>`: `data-testid={testId && `${testId}.image`}`
   - Summary `<p>`: `data-testid={testId && `${testId}.summary`}`
   - Meta section: `data-testid={testId && `${testId}.meta`}`
   - Kicker superheading: `data-testid={testId && `${testId}.kicker`}`
   - Header addon: `data-testid={testId && `${testId}.header-addon`}`
3. `ContentGrid.tsx`: Add optional `testId?: string` prop
   - Root grid `<div>`: `data-testid={testId}` (only when `testId` provided)
   - Empty state `<p>`: `data-testid={testId && `${testId}.empty`}` or `data-testid="content-grid.empty"`
4. `ContentPageShell.tsx`: Add optional `testId?: string` prop
   - Root container: `data-testid={testId}`
   - Eyebrow: `data-testid={testId && `${testId}.eyebrow`}`
   - Title `<h1>`: `data-testid={testId && `${testId}.title`}`
   - Description: `data-testid={testId && `${testId}.description`}`
   - Description trailing slot container: `data-testid={testId && `${testId}.description-trailing`}`
   - Content children wrapper: `data-testid={testId && `${testId}.content`}`
5. `ContentDetailShell.tsx`: Add optional `testId?: string` prop
   - Root container: `data-testid={testId}`
   - Lead media: `data-testid={testId && `${testId}.lead-media`}`
   - Eyebrow: `data-testid={testId && `${testId}.eyebrow`}`
   - Title `<h1>`: `data-testid={testId && `${testId}.title`}`
   - Summary: `data-testid={testId && `${testId}.summary`}`
   - Meta section: `data-testid={testId && `${testId}.meta`}`
   - Body content: `data-testid={testId && `${testId}.body`}`
   - After-content slot: `data-testid={testId && `${testId}.after-content`}`

### Phase 3.2 — Update callers to pass testId + instrument remaining components (3 parallel sub-agents)

**All 3 agents depend on Agent 3.1 completing first** (need the new `testId` prop type).

**Agent 3.2A — Courses**

| Property | Value |
|---|---|
| **Modifies** | `frontend/src/components/content/courses.tsx` |

**Scope:**
- `CourseList`: Pass `testId={join('egitimler', 'card', course.slug)}` to each `ContentCardShell`. Also add `data-testid="egitimler.catalog.empty"` on empty state (inside ContentGrid wrapper or parent).
- `CourseDetail`: Pass `testId="egitimler.detail"` to `ContentDetailShell`. Add `data-testid={join('egitimler', 'detail', 'teacher-link', course.teacher.slug)}` on teacher link. Add section selectors: `data-testid="egitimler.detail.section.description"` etc.
- Use `join` from `@/lib/testids`.

**Agent 3.2B — Events**

| Property | Value |
|---|---|
| **Modifies** | `frontend/src/components/content/events.tsx` |

**Scope:**
- `EventList`: Pass `testId={join('etkinlikler', 'card', event.slug)}` to each `ContentCardShell`. Add card meta sub-selectors where the explorer identified date/location.
- `EventDetail`: Pass `testId="etkinlikler.detail"` to `ContentDetailShell`. Add info panel selectors: `data-testid="etkinlikler.detail.info-panel"`, etc.
- Use `join` from `@/lib/testids`.

**Agent 3.2C — Blog, News, TeacherCard**

| Property | Value |
|---|---|
| **Modifies** | `frontend/src/components/content/blog.tsx`, `frontend/src/components/content/news.tsx`, `frontend/src/components/teacher-card.tsx` |

**Scope:**
1. `blog.tsx`: `BlogList` passes `testId={join('blog-yazilari', 'card', post.slug)}`. `BlogDetail` uses fixed selectors: `data-testid="blog-yazilari.detail.*"` (this component does NOT use ContentDetailShell — it's a custom layout). Add selectors for: `.cover-image`, `.title`, `.meta`, `.body`, `.source-notes`, `.after-content`.
2. `news.tsx`: `NewsList` passes `testId={join('haberler', 'card', item.id)}` (uses `id` since NewsListItem has no `slug`). Add `.tag` and `.published-date` sub-elements in meta.
3. `teacher-card.tsx`: Add inline selectors (not using shared shell): `data-testid={join('egitmenler', 'card', slug)}` on root link, `.name`, `.photo`, `.headline`, `.expertise.{area}` on sub-elements.

**Validation for Unit 3:** After both phases complete, verify with `content-testids-source.test.mjs`.

---

## Unit 4: Route-Level Selectors (3 parallel sub-agents, after Unit 3)

**Depends on:** Unit 1 (`testids.ts`), Unit 3 (shared components have testId prop)
**15 page files, zero overlap** between sub-agents.

### Sub-agent 4A — Home + Static Pages (4 files)

| Property | Value |
|---|---|
| **Modifies** | `page.tsx`, `hakkimizda/page.tsx`, `kvkk/page.tsx`, `cozum-ortagi/page.tsx` |

**Scope:**
1. `app/page.tsx` (Home): `data-testid="page.home"` on root section/container.
   - Hero CTA "Kurumsal Eğitim": `data-testid="page.home.hero.cta.corporate-training"` — **preserve** `data-measurement-id="home-hero-primary"`
   - Hero CTA "Kataloğu İncele": `data-testid="page.home.hero.cta.catalog"` — **preserve** `data-measurement-id="home-hero-secondary"`
   - Value prop cards: `data-testid="page.home.value-props.card.{slug}"` (use slugified title)
   - Visual story items: `data-testid="page.home.visual-story.item.{index}"` (static items, index acceptable here)
2. `app/hakkimizda/page.tsx`:
   - Root: `data-testid="page.hakkimizda"`
   - CTAs: `data-testid="page.hakkimizda.cta.*"` — **preserve** `data-measurement-id`
   - Carousel prev/next: `data-testid="page.hakkimizda.teacher-carousel.prev/next"` — **preserve** `aria-label`
   - Carousel cards: `data-testid={join('page', 'hakkimizda', 'teacher-carousel', 'card', teacher.slug)}`
   - Section selectors: `.section.netas-trust`, `.section.applied-learning`, etc.
3. `app/kvkk/page.tsx`: Root `data-testid="page.kvkk"`, external link `data-testid="page.kvkk.external-link.netas"`
4. `app/cozum-ortagi/page.tsx`: Root `data-testid="page.cozum-ortagi"`, CTA `data-testid="page.cozum-ortagi.cta.apply"`, area cards `data-testid="page.cozum-ortagi.area-card.{title-slug}"`

### Sub-agent 4B — Eğitimler + Eğitmenler + Haberler (5 files)

| Property | Value |
|---|---|
| **Modifies** | `egitimler/page.tsx`, `egitimler/[slug]/page.tsx`, `egitmenler/page.tsx`, `egitmenler/[slug]/page.tsx`, `haberler/page.tsx` |

**Scope:**
1. `app/egitimler/page.tsx`: Root `data-testid="page.egitimler"`. Pass `testId="page.egitimler"` to `ContentPageShell`. Add filter pill selectors using topic area keys from `TOPIC_AREAS` map. PDF disabled CTA: `data-testid="page.egitimler.catalog-pdf-download"`. Pass empty message to ContentGrid.
2. `app/egitimler/[slug]/page.tsx`: Root `data-testid="page.course-detail"`. Teacher link: `data-testid={join('page', 'course-detail', 'teacher-link', teacherSlug)}`. Corporate CTA: `data-testid="page.course-detail.cta.corporate-request"`. Section selectors for description, business-value, outcomes, scope, related-events. Related event links: `data-testid={join('page', 'course-detail', 'related-event', event.slug)}`.
3. `app/egitmenler/page.tsx`: Root `data-testid="page.egitmenler"`. Pass `testId="page.egitmenler"` to `ContentPageShell`. Empty state via ContentGrid.
4. `app/egitmenler/[slug]/page.tsx`: Root `data-testid="page.teacher-detail"`. Photo, email link, sections (expertise-areas, target-teams, teaching-approach, bio). Course links: `data-testid={join('page', 'teacher-detail', 'course', courseSlug)}`.
5. `app/haberler/page.tsx`: Root `data-testid="page.haberler"`. Pass `testId`. Empty state (already always-empty — just add selector).

### Sub-agent 4C — Etkinlikler + Blog + İletişim (6 files)

| Property | Value |
|---|---|
| **Modifies** | `etkinlikler/page.tsx`, `etkinlikler/[slug]/page.tsx`, `etkinlikler/[slug]/kayit/page.tsx`, `blog-yazilari/page.tsx`, `blog-yazilari/[slug]/page.tsx`, `iletisim/page.tsx` |

**Scope:**
1. `app/etkinlikler/page.tsx`: Root `data-testid="page.etkinlikler"`. Filter pills: `data-testid={join('page', 'etkinlikler', 'filter', 'type', typeKey)}`. Sort toggle: `data-testid="page.etkinlikler.sort.toggle"` — **preserve** `aria-label`. Pass `testId` to EventList shell.
2. `app/etkinlikler/[slug]/page.tsx`: Root `data-testid="page.event-detail"`. Register CTA: `data-testid="page.event-detail.register-cta"`. Contact CTA: `data-testid="page.event-detail.contact-cta"`. Registration-closed notice: `data-testid="page.event-detail.registration-closed.notice"`. Info panel: `data-testid="page.event-detail.info-panel"`. Newsletter form in closed state: `data-testid="page.event-detail.registration-closed.newsletter"`.
3. `app/etkinlikler/[slug]/kayit/page.tsx`: Root `data-testid="page.event-registration"`. Back-to-detail button: `data-testid="page.event-registration.back-to-detail"`. Closed state: `data-testid="page.event-registration.closed-state"`. Form element: `data-testid="page.event-registration.form"` — but field-level selectors are handled by Unit 5 (form components).
4. `app/blog-yazilari/page.tsx`: Root `data-testid="page.blog"`. Pass `testId`. Empty state.
5. `app/blog-yazilari/[slug]/page.tsx`: Root `data-testid="page.blog-detail"`. Title, excerpt, meta, content, related-posts section and cards.
6. `app/iletisim/page.tsx`: Root `data-testid="page.iletisim"`. Form root `data-testid="page.iletisim.form"` — field-level selectors handled by Unit 5.

**Validation for Unit 4:** After all agents complete, verify with `route-testids-source.test.mjs`.

---

## Unit 5: Form Selectors (3 parallel sub-agents, after Unit 4)

**Depends on:** Unit 1 (`testids.ts`), Unit 4 (some pages reference form elements)
**Zero file overlap** between sub-agents.

### Sub-agent 5A — Event Registration Form

| Property | Value |
|---|---|
| **Modifies** | `frontend/src/components/event-registration-form.tsx` |

**Scope:**
- Form root: `data-testid="event-registration.form"`
- Success state: `data-testid="event-registration.success"`
- Error state: `data-testid="event-registration.error"`
- Field inputs: `data-testid="event-registration.field.first-name"` etc. — **preserve ALL** existing `id` attributes (`firstName`, `lastName`, `email`, `phone`, `tckn`, `notes`, `kvkkConsent`)
- KVKK link: `data-testid="event-registration.link.kvkk-disclosure"`
- Submit: `data-testid="event-registration.submit"`
- Loading state: same element, text changes — verify `disabled` prop is observable

### Sub-agent 5B — Contact Lead Form + Intent Field Sections

| Property | Value |
|---|---|
| **Modifies** | `frontend/src/components/contact/intent-lead-form.tsx`, `frontend/src/components/contact/intent-field-sections.tsx` |

**Scope:**
1. `intent-lead-form.tsx`:
   - Form root: `data-testid="contact-lead.form"`
   - Tab container: `data-testid="contact-lead.tabs"`
   - Each tab button: `data-testid={join('contact-lead', 'tab', leadType)}` — uses stable `LeadType` keys
   - Error banner: `data-testid="contact-lead.error"`
   - Field error messages: `data-testid={join('contact-lead', 'error', fieldName)}` (e.g., `.error.full-name`)
   - Success state: `data-testid="contact-lead.success"`
   - Success CTA: `data-testid="contact-lead.success-cta"`
   - New submission button: `data-testid="contact-lead.new-submission"`
   - **Preserve ALL** existing `id`/`htmlFor` pairs: `fullName`, `email`, `phone`, `company`, `message`, `kvkkConsent`
   - Submit: `data-testid="contact-lead.submit"`
2. `intent-field-sections.tsx`:
   - Interest topic input: `data-testid="contact-lead.field.interest-topic"` — **preserve** `id="interestTopic"`
   - Expertise areas textarea: `data-testid="contact-lead.field.expertise-areas"` — **preserve** `id="expertiseAreas"`
   - Company size input: `data-testid="contact-lead.field.company-size"` — **preserve** `id="companySize"`
   - Partnership details textarea: `data-testid="contact-lead.field.partnership-details"` — **preserve** `id="partnershipDetails"`

### Sub-agent 5C — Newsletter Subscription Form

| Property | Value |
|---|---|
| **Modifies** | `frontend/src/components/newsletter-subscription-form.tsx` |

**Scope:**
- Form root: `data-testid="newsletter.form"`
- Email input: `data-testid="newsletter.field.email"` — **ADD** `id="newsletter-email"` and `<label htmlFor="newsletter-email">` (currently missing — gap found in Unit 0)
- Submit: `data-testid="newsletter.submit"`
- Success: `data-testid="newsletter.success"`
- Error: `data-testid="newsletter.error"`

**Validation for Unit 5:** After all agents complete, verify with `form-testids-source.test.mjs`.

---

## Unit 6: Validation and Cleanup (2 parallel sub-agents, after Units 1–5)

**Depends on:** All previous units complete
**Zero file overlap** between sub-agents.

### Sub-agent 6A — Write focused source tests

| Property | Value |
|---|---|
| **Type** | `tester` (writes tests) |
| **Creates/Modifies** | `frontend/src/__tests__/site-shell-testids-source.test.mjs`, `content-testids-source.test.mjs`, `route-testids-source.test.mjs`, `form-testids-source.test.mjs` |

**Scope:**
Create `.mjs` source tests (ESM, runnable with `node`) that verify:
- **Shell:** Header logo has `site-header.logo-link`, mobile toggle has `aria-controls`, desktop nav item has route-key-based testid, footer nav items have testids, breadcrumbs current has `aria-current="page"`
- **Content:** ContentCardShell forwards testId to root element and derives child selectors, ContentGrid shows empty state selector, search field has toggle + input selectors
- **Routes:** Critical pages expose root selectors, dynamic cards use slug-based selectors, filters/sort exist
- **Forms:** All form fields have testids, existing `id` attributes still present, submit/success/error selectors exist, KVKK links preserved

Do NOT test:
- Visual layout (no Tailwind class assertions)
- Exact text content (Turkish copy may change)
- Non-critical decorative elements

### Sub-agent 6B — Lint + Build verification

| Property | Value |
|---|---|
| **Type** | `general` |
| **Runs** | `npm run lint` then `npm run build:frontend` |

**Scope:**
1. Run `npm run lint` from repo root. Fix any lint errors introduced by the instrumentation changes.
2. Run `npm run build:frontend` from repo root. Fix any TypeScript/build errors.
3. If both pass clean, report success. If failures exist, fix and re-run.
4. Do NOT commit, push, or edit config files.

---

## Execution Order Summary

```
Unit 0: Explorer pass ✅ DONE (5 parallel agents)

  ⬇ (review inventory)

Unit 1: Sub-agent 1.1 (testids.ts + test)

  ⬇

Unit 2: Sub-agent 2A ║ Sub-agent 2B (parallel, 0 overlapping files)

  ⬇

Unit 3:
  Phase 3.1: Agent 3.1 (shared components testId prop)
    ⬇
  Phase 3.2: Agent 3.2A ║ Agent 3.2B ║ Agent 3.2C (parallel, 0 overlapping files)

  ⬇

Unit 4: Agent 4A ║ Agent 4B ║ Agent 4C (parallel, 0 overlapping files)

  ⬇

Unit 5: Agent 5A ║ Agent 5B ║ Agent 5C (parallel, 0 overlapping files)

  ⬇

Unit 6: Agent 6A ║ Agent 6B (parallel, 0 overlapping files)
```

**Total sub-agents: 16** across 6 units
**Max parallelism: 4 agents in parallel** (Unit 0, Unit 3 phase 2, Unit 4, Unit 5)

---

## Files Modified by Each Agent (Collision Check)

| Agent | Files |
|---|---|
| 1.1 | `frontend/src/lib/testids.ts` (create), `frontend/src/__tests__/testids-source.test.mjs` (create) |
| 2A | `frontend/src/app/layout.tsx`, `frontend/src/components/site-header.tsx` |
| 2B | `frontend/src/components/site-footer.tsx`, `frontend/src/components/breadcrumbs.tsx` |
| 3.1 | `frontend/src/components/content/content-card-shell.tsx`, `content-grid.tsx`, `content-page-shell.tsx`, `content-detail-shell.tsx`, `search-field.tsx` |
| 3.2A | `frontend/src/components/content/courses.tsx` |
| 3.2B | `frontend/src/components/content/events.tsx` |
| 3.2C | `frontend/src/components/content/blog.tsx`, `news.tsx`, `frontend/src/components/teacher-card.tsx` |
| 4A | `frontend/src/app/page.tsx`, `frontend/src/app/hakkimizda/page.tsx`, `frontend/src/app/kvkk/page.tsx`, `frontend/src/app/cozum-ortagi/page.tsx` |
| 4B | `frontend/src/app/egitimler/page.tsx`, `frontend/src/app/egitimler/[slug]/page.tsx`, `frontend/src/app/egitmenler/page.tsx`, `frontend/src/app/egitmenler/[slug]/page.tsx`, `frontend/src/app/haberler/page.tsx` |
| 4C | `frontend/src/app/etkinlikler/page.tsx`, `frontend/src/app/etkinlikler/[slug]/page.tsx`, `frontend/src/app/etkinlikler/[slug]/kayit/page.tsx`, `frontend/src/app/blog-yazilari/page.tsx`, `frontend/src/app/blog-yazilari/[slug]/page.tsx`, `frontend/src/app/iletisim/page.tsx` |
| 5A | `frontend/src/components/event-registration-form.tsx` |
| 5B | `frontend/src/components/contact/intent-lead-form.tsx`, `frontend/src/components/contact/intent-field-sections.tsx` |
| 5C | `frontend/src/components/newsletter-subscription-form.tsx` |
| 6A | `frontend/src/__tests__/site-shell-testids-source.test.mjs`, `content-testids-source.test.mjs`, `route-testids-source.test.mjs`, `form-testids-source.test.mjs` (create) |
| 6B | None (read-only validation, only fixes if needed) |

**Zero file collisions confirmed.** No two agents modify the same file.

