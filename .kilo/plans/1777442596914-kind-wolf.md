# Parallel Subagent Execution — Remaining Units

## Audit Summary

WAVE 1 (U01-U06): ✅ ALL COMPLETE
WAVE 2 done: U07, U08, U12 ✅   |   NOT done: U09, U10, U11 ❌
WAVE 3: U13, U14, U15 — NOT STARTED

## File Conflict Map

Only two shared files across U09/U10/U11:

1. `frontend/src/lib/strapi.ts` — Each unit patches its own non-overlapping section:
   - U09: `StrapiEvent` type + `getEvents()`/`getEventBySlug()` — add `details` to list query
   - U10: `StrapiBlogPost` type + `getBlogPosts()`/`getBlogPostBySlug()` — add author/date/sourceNotes, sort by publishedDate:desc
   - U11: `StrapiTeacher` type + `getTeachers()`/`getTeacherBySlug()` — add expertiseAreas/targetTeams/teachingApproach, remove console.log

2. `frontend/src/components/site-header.tsx` — OWNED BY U11 ONLY. U11 adds `/egitmenler` and `/cozum-ortagi` to navigationItems.

## Parallel Launch: U09 ∥ U10 ∥ U11

All three can run simultaneously. No write conflicts beyond the two coordinated files above.

### U09 — Events Frontend

Depends on: U03 (newsletter API exists), U05 (event schema has topicArea/keepRegistrationsOpen/details)

Goals:
- Rich text renderer component
- Value-first listing (summary before date)
- Registration-state CTA (open→register, closed→newsletter)
- Closed-event newsletter fallback

Files to change:
- `frontend/src/lib/strapi.ts` — add `details` field to `getEvents()` query
- `frontend/src/components/content/rich-text-content.tsx` — CREATE: constrained Strapi richtext renderer
- `frontend/src/components/content/events.tsx` — reorder card: summary before date meta in ContentCardShell
- `frontend/src/app/etkinlikler/page.tsx` — fix Turkish diacritics in description text
- `frontend/src/app/etkinlikler/[slug]/page.tsx` — use `isEventRegistrationOpen()`, conditional CTA
- `frontend/src/components/newsletter-subscription-form.tsx` — CREATE: subscribe form with source context
- Tests: `events-list-source.test.mjs`, `event-detail-source.test.mjs`, `event-newsletter-fallback-source.test.mjs`

Do NOT touch: site-header.tsx, strapi.ts blog/teacher sections

### U10 — Blog Frontend

Depends on: U06 (blog-author exists, blog-post has author/publishedDate/sourceNotes)

Goals:
- Update Strapi contract for author/date/source fields
- Title-searchable, newest-first blog list
- Detail page with author context, source notes, related posts

Files to change:
- `frontend/src/lib/strapi.ts` — `StrapiBlogPost` add author/publishedDate/sourceNotes/coverImage; `getBlogPosts()` sort publishedDate:desc + populate author; `getBlogPostBySlug()` add author + sourceNotes + coverImage
- `frontend/src/app/blog-yazilari/page.tsx` — client search island, author/date on cards, newest-first
- `frontend/src/app/blog-yazilari/[slug]/page.tsx` — author name/role/bio, sourceNotes block, related posts
- `frontend/src/components/content/blog.tsx` — update BlogList/BlogDetail wrappers for new fields
- Tests: `blog-strapi-contract-source.test.mjs`, `blog-discovery-source.test.mjs`, `blog-detail-source.test.mjs`

Do NOT touch: site-header.tsx, strapi.ts event/teacher sections

### U11 — Teacher Frontend

Depends on: U06 (teacher schema has expertiseAreas/targetTeams/teachingApproach)

Goals:
- Update Strapi contract for narrative fields
- Create /egitmenler listing page
- Update detail page hierarchy
- Add routes to site header

Files to change:
- `frontend/src/lib/strapi.ts` — `StrapiTeacher` add expertiseAreas/targetTeams/teachingApproach; `getTeachers()` + `getTeacherBySlug()` request new fields; REMOVE console.log debug from getTeacherBySlug
- `frontend/src/app/egitmenler/page.tsx` — CREATE: server-rendered listing with getTeachers()
- `frontend/src/components/teacher-card.tsx` — CREATE: expertise tags (max 3), targetTeams preview, photo/initials
- `frontend/src/app/egitmenler/[slug]/page.tsx` — reorder to: expertise → targetTeams → teachingApproach → courses → bio
- `frontend/src/components/site-header.tsx` — add `/egitmenler` and `/cozum-ortagi` to navigationItems
- Tests: `teacher-listing-contract-source.test.mjs`, `teacher-listing-page-source.test.mjs`, `teacher-detail-narrative-source.test.mjs`

Do NOT touch: strapi.ts event/blog sections

## After U09/U10/U11 Complete

Launch U13 (frontend instrumentation) and U14 (backend integration) in parallel.
Then U15 (cross-stack validation) as final pass.
