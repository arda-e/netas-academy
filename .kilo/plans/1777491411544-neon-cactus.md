# Delegation Plan: `data-testid` Instrumentation — Units 3–6

**Source plan:** `.kilo/plans/1777490191604-swift-circuit.md`
**Date:** 2026-04-29
**Status:** Plan ready for execution. Units 1–2 already complete.

---

## Current State

| Unit | Description | Status |
|---|---|---|
| Unit 1 | `testids.ts` helper + source test | ✅ DONE |
| Unit 2A | Header + layout selectors | ✅ DONE |
| Unit 2B | Footer + breadcrumbs selectors | ✅ DONE |
| Unit 3.1 | Add `testId` prop to shared content shells | ❌ TODO |
| Unit 3.2A | Courses caller testIds | ❌ TODO |
| Unit 3.2B | Events caller testIds | ❌ TODO |
| Unit 3.2C | Blog/News/TeacherCard testIds | ❌ TODO |
| Unit 4A | Home + Static page selectors | ❌ TODO |
| Unit 4B | Eğitimler/Eğitmenler/Haberler page selectors | ❌ TODO |
| Unit 4C | Etkinlikler/Blog/İletişim page selectors | ❌ TODO |
| Unit 5A | Event Registration Form selectors | ❌ TODO |
| Unit 5B | Contact Lead Form + Field Sections selectors | ❌ TODO |
| Unit 5C | Newsletter Subscription Form selectors | ❌ TODO |
| Unit 6A | Source tests (shell, content, route, form) | ❌ TODO |
| Unit 6B | Lint + Build verification | ❌ TODO |

**Already instrumented files (5):**
- `layout.tsx` — `data-testid="root-layout.content"`
- `site-header.tsx` — logo-link, mobile-toggle, desktop-nav, mobile-nav, all nav links
- `site-footer.tsx` — site-plan, legal, all link testids
- `breadcrumbs.tsx` — nav, link crumbs, current span
- `search-field.tsx` — toggle button (input still needs testid)

**Helper library:** `frontend/src/lib/testids.ts` exports `join()` and `normalizeKey()`. Already imported and used by the 5 instrumented files.

**UI primitives:** All 7 UI components forward `data-testid` via `{...props}` spreading — NO changes needed.

---

## Execution Order

```
Phase 1: Agent 3.1 (shared shells testId prop) — single agent, blocks Phase 2
   ⬇
Phase 2: Agents 3.2A ║ 3.2B ║ 3.2C (content callers, parallel, 0 file overlap)
   ⬇
Phase 3: Agents 4A ║ 4B ║ 4C (route pages, parallel, 0 file overlap)
   ⬇
Phase 4: Agents 5A ║ 5B ║ 5C (form components, parallel, 0 file overlap)
   ⬇
Phase 5: Agent 6A ║ Agent 6B (validation, parallel, 0 file overlap)
```

**File collision check:** No two agents modify the same file.

---

## Phase 1 — Agent 3.1: Shared Shell + SearchField `testId` prop

**Type:** `general`
**Depends on:** Nothing (Unit 1 helper already exists)
**Blocks:** Phase 2 (Agents 3.2A–C)

### Files to modify:
1. `frontend/src/components/content/content-card-shell.tsx`
2. `frontend/src/components/content/content-grid.tsx`
3. `frontend/src/components/content/content-page-shell.tsx`
4. `frontend/src/components/content/content-detail-shell.tsx`
5. `frontend/src/components/content/search-field.tsx` (add input testid)

### Prompt:

```
You are implementing Phase 3.1 of the data-testid instrumentation plan.

### Context
- Import `join` from `@/lib/testids` where needed (already exported, no React deps)
- All UI primitives (Card, Button, Input, etc.) forward `data-testid` via `{...props}` — you do NOT need to modify them
- Existing `aria-*` and `id`/`htmlFor` attributes must NOT be removed or changed
- Follow the pattern in already-instrumented files: `site-header.tsx`, `site-footer.tsx`, `breadcrumbs.tsx`

### Task 1: ContentCardShell (`content-card-shell.tsx`)
Add an optional `testId?: string` prop. When provided:
- Root: `<Link>` or `<Card>` gets `data-testid={testId}`
- Title `<CardTitle>`: `data-testid={testId && join(testId, 'title')}`
- Image container div: `data-testid={testId && join(testId, 'image')}`
- Summary `<p>` (when string): `data-testid={testId && join(testId, 'summary')}`
- Meta `<div>`: `data-testid={testId && join(testId, 'meta')}`
- Kicker `<ContentSuperheading>`: `data-testid={testId && join(testId, 'kicker')}`
- Header addon `<div>`: `data-testid={testId && join(testId, 'header-addon')}`

Only set `data-testid` when `testId` is truthy (don't create empty attributes).

### Task 2: ContentGrid (`content-grid.tsx`)
Add an optional `testId?: string` prop. When provided:
- Root `<div>`: `data-testid={testId}`
- Empty state `<p>`: `data-testid={testId && join(testId, 'empty')}`

When `testId` is NOT provided but `itemsCount === 0`, still add `data-testid="content-grid.empty"` for the universal empty state selector.

### Task 3: ContentPageShell (`content-page-shell.tsx`)
Add an optional `testId?: string` prop. When provided:
- Root `<main>`: `data-testid={testId}`
- Eyebrow `<p>`: `data-testid={testId && join(testId, 'eyebrow')}`
- Title `<h1>`: `data-testid={testId && join(testId, 'title')}`
- Description `<div>`: `data-testid={testId && join(testId, 'description')}`
- Description trailing container: `data-testid={testId && join(testId, 'description-trailing')}`
- Content children wrapper `<div>` (the page-section child wrapper): `data-testid={testId && join(testId, 'content')}`

### Task 4: ContentDetailShell (`content-detail-shell.tsx`)
Add an optional `testId?: string` prop. When provided:
- Root `<main>`: `data-testid={testId}`
- Lead media `<div>`: `data-testid={testId && join(testId, 'lead-media')}`
- Eyebrow `<p>`: `data-testid={testId && join(testId, 'eyebrow')}`
- Title `<h1>`: `data-testid={testId && join(testId, 'title')}`
- Summary `<p>`: `data-testid={testId && join(testId, 'summary')}`
- Meta `<div>`: `data-testid={testId && join(testId, 'meta')}`
- Body content `<div>` (the prose wrapper): `data-testid={testId && join(testId, 'body')}`
- After-content slot: `data-testid={testId && join(testId, 'after-content')}`

### Task 5: SearchField (`search-field.tsx`)
Add `data-testid="search-field.input"` to the `<input>` element. The toggle button already has `data-testid="search-field.toggle"` — do NOT touch that.

### Critical rules:
- Do NOT change any existing `aria-*`, `id`, `htmlFor`, or `data-measurement-id` attributes
- Do NOT change the component's visual styling (Tailwind classes)
- Do NOT change component behavior or props interfaces except adding `testId`
- Import `join` from `@/lib/testids` (it's a pure function, no React dependency)
- Verify each file compiles by checking the diff carefully — no syntax errors

After implementing all 5 files, return a summary of:
1. Which files were modified
2. What new props were added
3. Any edge cases you handled
4. A list of all new data-testid selectors added (file by file)
```

### Validation:
After completion, manually verify:
- `npm run lint` (from repo root) passes with zero new errors
- Review diff to confirm no existing `aria-*` or `id` attributes were removed
- Confirm `testId` prop is optional (doesn't break existing callers)

---

## Phase 2 — Agents 3.2A, 3.2B, 3.2C (parallel)

**All depend on:** Phase 1 complete (shell components have `testId` prop)
**Zero file overlap between agents.**

---

### Agent 3.2A — Courses

**Type:** `general`
**Modifies:** `frontend/src/components/content/courses.tsx`
**Prompt:**

```
You are adding data-testid attributes to the Courses content components.

### Context
- `testids.ts` exports `join(...)` — already at `@/lib/testids`
- `ContentCardShell`, `ContentGrid`, `ContentDetailShell` now accept an optional `testId` prop (from Phase 3.1)
- `course.slug` is always available on course items

### Task: `courses.tsx`

1. **CourseList** — Pass `testId={join('egitimler', 'card', course.slug)}` to each `ContentCardShell`.
   Also add `data-testid="egitimler.catalog.empty"` to the empty state. The simplest approach: wrap ContentGrid or add the prop. Since ContentGrid already supports `testId`, pass `testId="egitimler.catalog"` to ContentGrid. It will render `data-testid="egitimler.catalog.empty"` when empty.

2. **CourseDetail** — Pass `testId="egitimler.detail"` to `ContentDetailShell`.

### Critical rules:
- Do NOT change any existing props or behavior
- Do NOT remove any existing `aria-*` attributes
- Import `join` from `@/lib/testids` if not already imported
- Only add `data-testid` attributes — no style changes, no logic changes

Return a summary of exactly which lines were added/changed.
```

---

### Agent 3.2B — Events

**Type:** `general`
**Modifies:** `frontend/src/components/content/events.tsx`
**Prompt:**

```
You are adding data-testid attributes to the Events content components.

### Context
- `testids.ts` exports `join(...)` — already at `@/lib/testids`
- `ContentCardShell`, `ContentGrid`, `ContentDetailShell` now accept an optional `testId` prop (from Phase 3.1)
- `event.slug` is always available on event items

### Task: `events.tsx`

1. **EventList** — Pass `testId={join('etkinlikler', 'card', event.slug)}` to each `ContentCardShell`.
   Pass `testId="etkinlikler.list"` to `ContentGrid` (so empty state renders `data-testid="etkinlikler.list.empty"`).

2. **EventDetail** — Pass `testId="etkinlikler.detail"` to `ContentDetailShell`.

### Critical rules:
- Do NOT change any existing props or behavior
- Import `join` from `@/lib/testids` if not already imported
- Only add `data-testid` attributes — no style changes, no logic changes

Return a summary of exactly which lines were added/changed.
```

---

### Agent 3.2C — Blog, News, TeacherCard

**Type:** `general`
**Modifies:** `frontend/src/components/content/blog.tsx`, `frontend/src/components/content/news.tsx`, `frontend/src/components/teacher-card.tsx`
**Prompt:**

```
You are adding data-testid attributes to Blog, News, and TeacherCard components.

### Context
- `testids.ts` exports `join(...)` — already at `@/lib/testids`
- `ContentCardShell`, `ContentGrid` now accept an optional `testId` prop (from Phase 3.1)
- Blog/News items have `.slug` or `.id`; teacher cards have `slug`

### Task 1: `blog.tsx`

1. **BlogList** — Pass `testId={join('blog-yazilari', 'card', post.slug)}` to each `ContentCardShell`.
   Pass `testId="blog-yazilari.list"` to `ContentGrid` (for empty state).

2. **BlogDetail** (custom layout, NOT using ContentDetailShell):
   - Add `data-testid="blog-yazilari.detail"` on the root `<main>`
   - Cover image div: `data-testid="blog-yazilari.detail.cover-image"`
   - Title `<h1>`: `data-testid="blog-yazilari.detail.title"`
   - Excerpt `<p>`: `data-testid="blog-yazilari.detail.excerpt"`
   - Meta div: `data-testid="blog-yazilari.detail.meta"`
   - Body content article: `data-testid="blog-yazilari.detail.body"`
   - Source notes div: `data-testid="blog-yazilari.detail.source-notes"`
   - After-content section: `data-testid="blog-yazilari.detail.after-content"`

### Task 2: `news.tsx`

1. **NewsList** — Pass `testId={join('haberler', 'card', String(item.id))}` to each `ContentCardShell` (NewsListItem has `id` not `slug`).
   Pass `testId="haberler.list"` to `ContentGrid`.

### Task 3: `teacher-card.tsx`

Add inline selectors (not using shared shell):
- Root `<Link>`: `data-testid={join('egitmenler', 'card', slug)}`
- Photo/initials container div: `data-testid={join('egitmenler', 'card', slug, 'photo')}`
- Name `<h3>`: `data-testid={join('egitmenler', 'card', slug, 'name')}`
- Headline `<p>`: `data-testid={join('egitmenler', 'card', slug, 'headline')}`
- Expertise area `<span>`: `data-testid={join('egitmenler', 'card', slug, 'expertise', normalizeKey(area))}`
  - Import `normalizeKey` from `@/lib/testids` for this

### Critical rules:
- Do NOT change existing props, behavior, or styles
- Do NOT remove any `aria-*` attributes
- Import `join` and `normalizeKey` from `@/lib/testids`

Return a summary of exactly which lines were added/changed in each file.
```

---

## Phase 3 — Agents 4A, 4B, 4C (parallel)

**All depend on:** Phase 2 complete (content callers pass testId)
**Zero file overlap between agents.**

---

### Agent 4A — Home + Static Pages

**Type:** `general`
**Modifies:** `frontend/src/app/page.tsx`, `frontend/src/app/hakkimizda/page.tsx`, `frontend/src/app/kvkk/page.tsx`, `frontend/src/app/cozum-ortagi/page.tsx`
**Prompt:**

```
You are adding data-testid attributes to the Home page and 3 static pages.

### Context
- `testids.ts` exports `join()` and `normalizeKey()` at `@/lib/testids`
- Shared components (ContentPageShell, ContentDetailShell, ContentGrid, etc.) already accept optional `testId` prop
- These pages may use `data-measurement-id` — preserve ALL of them
- These pages may use `aria-*` attributes — preserve ALL of them

### Task 1: `app/page.tsx` (Home)
- Root section/container: `data-testid="page.home"`
- Hero CTA "Kurumsal Eğitim": `data-testid="page.home.hero.cta.corporate-training"` — **preserve any existing `data-measurement-id`**
- Hero CTA "Kataloğu İncele": `data-testid="page.home.hero.cta.catalog"` — **preserve any existing `data-measurement-id`**
- Value prop cards: `data-testid={join('page', 'home', 'value-props', 'card', normalizeKey(titleSlug))}`
- Visual story items: `data-testid={join('page', 'home', 'visual-story', 'item', String(index))}` (static items, index acceptable)
- If the page uses `ContentPageShell`, pass `testId="page.home"`

### Task 2: `app/hakkimizda/page.tsx`
- Root: `data-testid="page.hakkimizda"`
- CTAs: `data-testid="page.hakkimizda.cta.*"` — **preserve `data-measurement-id` attributes** (e.g., `about_corporate_cta`, `about_education_cta`)
- Teacher carousel cards: `data-testid={join('page', 'hakkimizda', 'teacher-carousel', 'card', teacher.slug)}`
- Carousel prev/next buttons: `data-testid="page.hakkimizda.teacher-carousel.prev"` / `.next` — **preserve `aria-label`**
- Course carousel cards: `data-testid={join('page', 'hakkimizda', 'course-carousel', 'card', course.slug)}`
- Section selectors: `data-testid="page.hakkimizda.section.netas-trust"`, `.applied-learning`, etc.
- If the page uses `ContentPageShell`, pass `testId="page.hakkimizda"`

### Task 3: `app/kvkk/page.tsx`
- Root: `data-testid="page.kvkk"`
- External Netas link: `data-testid="page.kvkk.external-link.netas"`
- Commit ID (debug element, if present): `data-testid="page.kvkk.commit-id"`

### Task 4: `app/cozum-ortagi/page.tsx`
- Root: `data-testid="page.cozum-ortagi"`
- Area cards: `data-testid={join('page', 'cozum-ortagi', 'area-card', normalizeKey(titleText))}`
- Apply CTA: `data-testid="page.cozum-ortagi.cta.apply"`

### Critical rules:
- Import `join` and `normalizeKey` from `@/lib/testids`
- Do NOT change any existing `data-measurement-id`, `aria-*`, or `id` attributes
- Do NOT change any visual styling or component behavior
- For pages that pass `testId` to shared shells, verify the shell prop name is `testId` (not `testid` or `dataTestId`)

After implementing, return:
1. Summary of each file's changes
2. Any existing attributes you had to preserve
3. List of new selectors added
```

---

### Agent 4B — Eğitimler/Eğitmenler/Haberler Pages

**Type:** `general`
**Modifies:** `frontend/src/app/egitimler/page.tsx`, `frontend/src/app/egitimler/[slug]/page.tsx`, `frontend/src/app/egitmenler/page.tsx`, `frontend/src/app/egitmenler/[slug]/page.tsx`, `frontend/src/app/haberler/page.tsx`
**Prompt:**

```
You are adding data-testid attributes to training, teacher, and news route pages.

### Context
- `testids.ts` exports `join()` and `normalizeKey()` at `@/lib/testids`
- Shared components (ContentPageShell, ContentDetailShell, ContentGrid, ContentCardShell) accept optional `testId` prop
- These pages may use `aria-*` — preserve ALL of them
- `course.slug`, `teacher.slug` are available in detail pages

### Task 1: `app/egitimler/page.tsx` (Course listing)
- Root: `data-testid="page.egitimler"`
- Pass `testId="page.egitimler"` to `ContentPageShell` (if used) or add to root container
- Filter pills use topic area keys — add `data-testid={join('page', 'egitimler', 'filter', 'topic', normalizeKey(areaKey))}`
- PDF download CTA (aria-disabled placeholder): `data-testid="page.egitimler.catalog-pdf-download"`
- Pass `testId` to CourseList/CourseGrid components if they accept it

### Task 2: `app/egitimler/[slug]/page.tsx` (Course detail)
- Root: `data-testid="page.course-detail"`
- Pass `testId="page.course-detail"` to `ContentDetailShell` (if used) or add to root
- Teacher link: `data-testid={join('page', 'course-detail', 'teacher-link', teacherSlug)}`
- Corporate training CTA: `data-testid="page.course-detail.cta.corporate-request"`
- Section selectors: `data-testid="page.course-detail.section.description"`, `.business-value`, `.outcomes`, `.scope`
- Related event links: `data-testid={join('page', 'course-detail', 'related-event', event.slug)}`

### Task 3: `app/egitmenler/page.tsx` (Teacher listing)
- Root: `data-testid="page.egitmenler"`
- Pass `testId="page.egitmenler"` to `ContentPageShell` (if used)
- Pass testId to grid for empty state handling

### Task 4: `app/egitmenler/[slug]/page.tsx` (Teacher detail)
- Root: `data-testid="page.teacher-detail"`
- Photo element: `data-testid="page.teacher-detail.photo"`
- Email link: `data-testid="page.teacher-detail.email"`
- Course links: `data-testid={join('page', 'teacher-detail', 'course', courseSlug)}`
- Section selectors: `data-testid="page.teacher-detail.section.expertise-areas"`, `.target-teams`, `.teaching-approach`, `.bio`

### Task 5: `app/haberler/page.tsx` (News listing — currently always empty)
- Root: `data-testid="page.haberler"`
- Pass `testId="page.haberler"` to shared shell components
- Empty state selector via grid `testId` prop

### Critical rules:
- Import `join` and `normalizeKey` from `@/lib/testids`
- Do NOT change existing `aria-*` attributes (especially `aria-current` on filter pills, `aria-disabled` on PDF link)
- Do NOT change any styling or behavior
- Read each page file carefully before making changes — understand the component hierarchy to place testids on the right elements

Return a summary of each file's changes and all new selectors added.
```

---

### Agent 4C — Etkinlikler/Blog/İletişim Pages

**Type:** `general`
**Modifies:** `frontend/src/app/etkinlikler/page.tsx`, `frontend/src/app/etkinlikler/[slug]/page.tsx`, `frontend/src/app/etkinlikler/[slug]/kayit/page.tsx`, `frontend/src/app/blog-yazilari/page.tsx`, `frontend/src/app/blog-yazilari/[slug]/page.tsx`, `frontend/src/app/iletisim/page.tsx`
**Prompt:**

```
You are adding data-testid attributes to events, blog, and contact route pages.

### Context
- `testids.ts` exports `join()` and `normalizeKey()` at `@/lib/testids`
- Shared components accept optional `testId` prop
- These pages may use `aria-*` — preserve ALL of them

### Task 1: `app/etkinlikler/page.tsx` (Event listing)
- Root: `data-testid="page.etkinlikler"`
- Filter pills by type: `data-testid={join('page', 'etkinlikler', 'filter', 'type', typeKey)}`
  - Type keys: `etkinlik`, `egitim`, `kurs` (from URL query params)
- Sort toggle: `data-testid="page.etkinlikler.sort.toggle"` — **preserve `aria-label` if present**
- Pass `testId` to EventList/ContentGrid

### Task 2: `app/etkinlikler/[slug]/page.tsx` (Event detail)
- Root: `data-testid="page.event-detail"`
- Pass `testId="page.event-detail"` to `ContentDetailShell` (if used)
- Register CTA button: `data-testid="page.event-detail.register-cta"`
- Contact CTA: `data-testid="page.event-detail.contact-cta"`
- Registration-closed notice: `data-testid="page.event-detail.registration-closed.notice"`
- Info panel: `data-testid="page.event-detail.info-panel"`
- Newsletter form in closed state: `data-testid="page.event-detail.registration-closed.newsletter"`

### Task 3: `app/etkinlikler/[slug]/kayit/page.tsx` (Registration page)
- Root: `data-testid="page.event-registration"`
- Back-to-detail button: `data-testid="page.event-registration.back-to-detail"`
- Closed state container: `data-testid="page.event-registration.closed-state"`
- Form container: `data-testid="page.event-registration.form"` — field-level selectors are handled separately in form components; just tag the form wrapper

### Task 4: `app/blog-yazilari/page.tsx` (Blog listing)
- Root: `data-testid="page.blog"`
- Pass `testId="page.blog"` to shared shell
- Pass testId to BlogList/ContentGrid

### Task 5: `app/blog-yazilari/[slug]/page.tsx` (Blog detail)
- Root: `data-testid="page.blog-detail"`
- Title: `data-testid="page.blog-detail.title"`
- Excerpt: `data-testid="page.blog-detail.excerpt"`
- Meta: `data-testid="page.blog-detail.meta"`
- Content body: `data-testid="page.blog-detail.body"`
- Related posts section: `data-testid="page.blog-detail.related-posts"`
- Related post cards: `data-testid={join('page', 'blog-detail', 'related-posts', 'card', post.slug)}`

### Task 6: `app/iletisim/page.tsx` (Contact)
- Root: `data-testid="page.iletisim"`
- Form root container: `data-testid="page.iletisim.form"` — field-level selectors handled by form components

### Critical rules:
- Import `join` and `normalizeKey` from `@/lib/testids`
- Do NOT change existing `aria-*` (particularly `aria-current` on filters, `aria-label` on sort toggle)
- Do NOT change any styling or behavior
- Read each page file before editing to understand the exact component structure

Return a summary of each file's changes and all new selectors added.
```

---

## Phase 4 — Agents 5A, 5B, 5C (parallel)

**All depend on:** Phase 3 complete (route pages instrumented)
**Zero file overlap between agents.**

---

### Agent 5A — Event Registration Form

**Type:** `general`
**Modifies:** `frontend/src/components/event-registration-form.tsx`
**Prompt:**

```
You are adding data-testid attributes to the Event Registration Form component.

### Context
- `testids.ts` exports `join()` at `@/lib/testids`
- This form has existing `id` attributes: `firstName`, `lastName`, `email`, `phone`, `tckn`, `notes`, `kvkkConsent`
- ALL of these `id` attributes and their `htmlFor` labels MUST be preserved exactly as-is
- The `Input`, `Textarea`, and `Button` UI components forward `data-testid` via `{...props}`

### Task: `event-registration-form.tsx`

Add these data-testid attributes:

1. Form root `<form>`: `data-testid="event-registration.form"`
2. Success message `<div>`: `data-testid="event-registration.success"`
3. Error message `<div>`: `data-testid="event-registration.error"`
4. Field inputs — add `data-testid` prop to each `<Input>`/`<Textarea>`:
   - `data-testid="event-registration.field.first-name"` (firstName input)
   - `data-testid="event-registration.field.last-name"` (lastName input)
   - `data-testid="event-registration.field.email"` (email input)
   - `data-testid="event-registration.field.phone"` (phone input)
   - `data-testid="event-registration.field.tckn"` (tckn input)
   - `data-testid="event-registration.field.notes"` (notes textarea)
5. KVKK consent checkbox input: `data-testid="event-registration.field.kvkk-consent"`
6. KVKK disclosure link `<Link>`: `data-testid="event-registration.link.kvkk-disclosure"`
7. Submit button: `data-testid="event-registration.submit"` (the button text changes between "Kaydi Tamamla" and "Kayit Gonderiliyor...")

### Critical rules:
- Do NOT remove or change any `id` attributes (`firstName`, `lastName`, `email`, `phone`, `tckn`, `notes`, `kvkkConsent`)
- Do NOT change `htmlFor` labels, `name` attributes, or form behavior
- Do NOT change any styling
- The form is already a `'use client'` component with hooks — don't add unnecessary client/server boundaries

Return a summary of all new selectors added and confirm all `id` attributes were preserved.
```

---

### Agent 5B — Contact Lead Form + Intent Field Sections

**Type:** `general`
**Modifies:** `frontend/src/components/contact/intent-lead-form.tsx`, `frontend/src/components/contact/intent-field-sections.tsx`
**Prompt:**

```
You are adding data-testid attributes to the Contact Lead Form and Intent Field Sections components.

### Context
- `testids.ts` exports `join()` at `@/lib/testids`
- These forms have existing `id` attributes that MUST be preserved:
  - `intent-lead-form.tsx`: `fullName`, `email`, `phone`, `company`, `message`, `kvkkConsent`
  - `intent-field-sections.tsx`: `interestTopic`, `expertiseAreas`, `companySize`, `partnershipDetails`
- The `Input`, `Textarea`, and `Button` UI components forward `data-testid` via `{...props}`
- Contact tabs use `LeadType` keys: `corporate_training_request`, `instructor_application`, `solution_partner_application`, `general_contact`

### Task 1: `intent-lead-form.tsx`

Add these data-testid attributes:
1. Form root: `data-testid="contact-lead.form"`
2. Tab container: `data-testid="contact-lead.tabs"`
3. Each tab button: `data-testid={join('contact-lead', 'tab', leadType)}` where `leadType` is the stable enum key
4. Error banner: `data-testid="contact-lead.error"`
5. Field-specific error messages: `data-testid={join('contact-lead', 'error', fieldName)}` (e.g., `.error.full-name`)
6. Success state: `data-testid="contact-lead.success"`
7. Success CTA: `data-testid="contact-lead.success-cta"`
8. New submission button: `data-testid="contact-lead.new-submission"`
9. Submit button: `data-testid="contact-lead.submit"`
10. Each field input — add `data-testid` prop:
    - `data-testid="contact-lead.field.full-name"` (fullName input)
    - `data-testid="contact-lead.field.email"` (email input)
    - `data-testid="contact-lead.field.phone"` (phone input)
    - `data-testid="contact-lead.field.company"` (company input)
    - `data-testid="contact-lead.field.message"` (message textarea)
11. KVKK consent checkbox: `data-testid="contact-lead.field.kvkk-consent"`
12. KVKK disclosure link: `data-testid="contact-lead.link.kvkk-disclosure"`

### Task 2: `intent-field-sections.tsx`

Add data-testid to the 4 intent-specific fields:
1. Interest topic input: `data-testid="contact-lead.field.interest-topic"` — **preserve `id="interestTopic"`**
2. Expertise areas textarea: `data-testid="contact-lead.field.expertise-areas"` — **preserve `id="expertiseAreas"`**
3. Company size input: `data-testid="contact-lead.field.company-size"` — **preserve `id="companySize"`**
4. Partnership details textarea: `data-testid="contact-lead.field.partnership-details"` — **preserve `id="partnershipDetails"`**

### Critical rules:
- Do NOT remove or change any `id` attributes or `htmlFor` labels
- Do NOT change `name` attributes or form behavior
- Do NOT change any styling
- Read both files fully before making changes — understand the component structure

Return a summary of all new selectors added and confirm all `id`/`htmlFor` pairs were preserved.
```

---

### Agent 5C — Newsletter Subscription Form

**Type:** `general`
**Modifies:** `frontend/src/components/newsletter-subscription-form.tsx`
**Prompt:**

```
You are adding data-testid attributes AND fixing an accessibility gap in the Newsletter Subscription Form.

### Context
- `testids.ts` exports `join()` at `@/lib/testids`
- This form currently has NO `id` or `label` on the email input — this is an accessibility gap found in the Unit 0 inventory
- The `Input` and `Button` UI components forward `data-testid` via `{...props}`

### Task: `newsletter-subscription-form.tsx`

1. **Accessibility fix (required):** Add `id="newsletter-email"` to the email `<Input>` and wrap it with a `<label htmlFor="newsletter-email">` tag (visually hidden or visible — use `sr-only` if you prefer to hide it). This is currently missing.

2. **Testid attributes:**
   - Form root: `data-testid="newsletter.form"`
   - Email input: `data-testid="newsletter.field.email"` (add as prop to `<Input>`)
   - Submit button: `data-testid="newsletter.submit"` (add as prop to `<Button>`)
   - Success message: `data-testid="newsletter.success"` (on the success `<p>`)
   - Error message: `data-testid="newsletter.error"` (on the error `<p>`)

### Critical rules:
- Do NOT change existing form behavior (success/error/idle state management)
- Do NOT change styling
- The label can use `className="sr-only"` for screen-reader-only accessibility
- The form is already a `'use client'` component — keep it that way

Return a summary of all changes, confirming the label was added and all testids are in place.
```

---

## Phase 5 — Agents 6A and 6B (parallel)

**Depend on:** All previous phases complete
**Zero file overlap.**

---

### Agent 6A — Write Source Tests

**Type:** `tester`
**Creates:** `frontend/src/__tests__/site-shell-testids-source.test.mjs`, `content-testids-source.test.mjs`, `route-testids-source.test.mjs`, `form-testids-source.test.mjs`
**Prompt:**

```
You are writing focused source tests for the data-testid instrumentation.

### Context
- Tests are `.mjs` files runnable with `node` (ESM, using `node:assert` strict mode)
- Use the existing `testids-source.test.mjs` as a style reference — it re-implements testids functions inline
- Source tests verify source code patterns via regex/file-read, NOT by rendering React components
- Each test file should import from `node:assert` and `node:fs`, read the source files as strings, and assert patterns exist

### File 1: `site-shell-testids-source.test.mjs`

Verify (by reading source files as strings):
- `layout.tsx` contains `data-testid="root-layout.content"`
- `site-header.tsx` contains `data-testid="site-header.logo-link"`
- `site-header.tsx` contains `aria-controls="site-mobile-navigation"` (preserved)
- `site-header.tsx` contains `data-testid="site-header.mobile-menu.toggle"`
- `site-header.tsx` contains `data-testid="site-header.desktop-nav"`
- `site-footer.tsx` contains `data-testid="site-footer.site-plan"`
- `site-footer.tsx` contains `data-testid="site-footer.legal"`
- `breadcrumbs.tsx` contains `data-testid="breadcrumbs.nav"`
- `breadcrumbs.tsx` contains `aria-current="page"` (preserved)

### File 2: `content-testids-source.test.mjs`

Verify:
- `content-card-shell.tsx` has `testId` in its props type definition
- `content-card-shell.tsx` uses `testId` in data-testid assignments
- `content-grid.tsx` has `testId` in its props type
- `content-grid.tsx` has `data-testid` on the empty state element
- `search-field.tsx` contains `data-testid="search-field.input"`
- `courses.tsx` imports `join` from `@/lib/testids` (or uses testId pattern)
- `teacher-card.tsx` contains data-testid attributes

### File 3: `route-testids-source.test.mjs`

Verify (read page source files):
- `app/page.tsx` contains `data-testid="page.home"`
- `app/hakkimizda/page.tsx` contains `data-testid="page.hakkimizda"`
- `app/hakkimizda/page.tsx` contains `data-measurement-id` (preserved, at least 1 occurrence)
- `app/egitimler/page.tsx` contains `data-testid="page.egitimler"`
- `app/etkinlikler/page.tsx` contains `data-testid="page.etkinlikler"`
- At least 3 detail pages have root data-testid attributes

### File 4: `form-testids-source.test.mjs`

Verify:
- `event-registration-form.tsx` contains `data-testid="event-registration.form"`
- `event-registration-form.tsx` still contains `id="firstName"` (preserved)
- `event-registration-form.tsx` still contains `htmlFor="firstName"` (preserved)
- `newsletter-subscription-form.tsx` contains `id="newsletter-email"` (newly added)
- `newsletter-subscription-form.tsx` contains `htmlFor="newsletter-email"` (newly added)
- `intent-lead-form.tsx` contains `data-testid="contact-lead.form"`
- `intent-lead-form.tsx` still contains `id="fullName"` (preserved)

### Test expectations:
- Do NOT test visual layout (no Tailwind class assertions)
- Do NOT test exact Turkish text content
- Do NOT test non-critical decorative elements
- Each test file should print "✅ All [suite-name] tests passed." on success
- Use `assert.ok(source.includes(pattern), message)` pattern
- Handle file-not-found gracefully with clear error messages

After writing all 4 files, run them with:
```bash
node frontend/src/__tests__/site-shell-testids-source.test.mjs
node frontend/src/__tests__/content-testids-source.test.mjs
node frontend/src/__tests__/route-testids-source.test.mjs
node frontend/src/__tests__/form-testids-source.test.mjs
```

Return a summary of all test files created and their pass/fail status.
```

---

### Agent 6B — Lint + Build Verification

**Type:** `general`
**Runs:** `npm run lint` then `npm run build:frontend` from repo root
**Prompt:**

```
You are performing the final lint and build verification for the data-testid instrumentation project.

### Task
1. Run `npm run lint` from the repo root (`/Users/arda/Desktop/development/netas_academy`).
   - If lint errors exist, categorize them:
     - **Pre-existing** (in files you didn't touch): Skip them, note them for the report
     - **Newly introduced**: Fix them if they're in instrumented files
   - Use `npm run lint` directly, not individual file linting

2. Run `npm run build:frontend` from the repo root.
   - This runs the Next.js build with TypeScript checking
   - If build errors exist:
     - Fix any TypeScript errors in instrumented files
     - Do NOT fix errors in unrelated files unless they block the build
   - If the build passes clean, report success

3. Do NOT:
   - Commit any changes
   - Push to remote
   - Edit config files (`.eslintrc`, `next.config`, `tsconfig`, etc.)
   - Add new dependencies

### Expected result:
- Zero new lint errors from instrumented files
- Build completes successfully (either clean or with only pre-existing warnings)

Return a report with:
1. Lint output summary (errors if any, categorized as pre-existing vs new)
2. Build output summary (success/failure, any errors fixed)
3. Final pass/fail status
```

---

## Summary

| Phase | Agents | Parallelism | Files Modified |
|---|---|---|---|
| Phase 1 | 3.1 | 1 agent | 4 shells + 1 search-field = 5 files |
| Phase 2 | 3.2A, 3.2B, 3.2C | 3 parallel | 5 files (1, 1, 3) |
| Phase 3 | 4A, 4B, 4C | 3 parallel | 15 files (4, 5, 6) |
| Phase 4 | 5A, 5B, 5C | 3 parallel | 4 files (1, 2, 1) |
| Phase 5 | 6A, 6B | 2 parallel | 4 test files created + 0 modified |

**Total sub-agents: 12** across 5 execution phases
**Max parallelism: 3 agents** (Phases 2, 3, 4)
**Total files instrumented: ~29 files** (plus 4 test files created)
**Total data-testid selectors expected: ~200**
