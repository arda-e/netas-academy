# PR Review Fixes: feat(u04): measurement backend

## Goal
Address the 6 issues identified in the PR review for the analytics-event backend feature (branch: `feat/u04-measurement-backend`).

## Files to modify
1. `backend/src/api/analytics-event/controllers/analytics-event.ts`
2. `backend/src/api/analytics-event/services/analytics-event.ts`
3. `backend/src/api/analytics-event/lib/constants.ts` (NEW)
4. `frontend/src/app/api/analytics/events/route.ts`

## Fixes → Subagent Delegation

### Task A: Extract shared constants/lib → Fix issues #1, #2, #3 (🔴)

**Subagent A**: Create `backend/src/api/analytics-event/lib/constants.ts` with:
- `KNOWN_EVENT_IDS` array
- `PII_PATTERNS` array
- `containsPII(key)` function
- `sanitizeProperties(properties)` function

Then update:
- **Controller** (`controllers/analytics-event.ts`): Remove `ALLOWED_PROPERTY_KEYS`, `PII_PATTERNS`, `containsPII`, `sanitizeProperties` (all dead code). Keep `KNOWN_EVENT_IDS` and eventId validation. Import shared `KNOWN_EVENT_IDS` from lib.
- **Service** (`services/analytics-event.ts`): Remove duplicate `KNOWN_EVENT_IDS`, `PII_PATTERNS`, `containsPII`, `sanitizeProperties`. Remove redundant validation (eventId already validated by controller). Import from lib. Keep PII stripping call and database creation.

The controller is the single validation entry point; the service trusts its caller.

Verify: existing tests (`backend/tests/api/analytics-event/routes.test.ts`, `service.test.ts`) must pass without modification to test logic (imports may need updating).

### Task B: Frontend proxy error handling → Fix issue #6 (🟡)

**Subagent B**: Wrap the `fetch` call in `frontend/src/app/api/analytics/events/route.ts` in try/catch to return a consistent JSON error response when Strapi is unreachable.

### Task C: Input length validation → Fix issue #5 (🟡)

**Subagent C**: Add basic length/type guards in the service's `capture()` method for string fields (`sessionId`, `pagePath`, `sourcePage`, `ctaId`, `contentType`, `contentSlug`, `backendReference`). Max length: 1000 chars per field. Reject oversized payloads with `ValidationError`.

### Task D: Rate limiting note → Fix issue #4 (🟡)

**Subagent D**: Add a comment in the route config file (`routes/custom-analytics-event.ts`) documenting that rate limiting is a known gap for U14 to address, since `auth: false` means public access.

## Validation

```bash
npm run build:backend
npm run lint
```

## Rollback
All changes are on `feat/u04-measurement-backend` branch. Revert with `git checkout main -- <file>` if needed.
